<UserControl
    x:Class="JitHub.Views.Controls.Activity.IssueCommentActivity"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:code="using:JitHub.Views.Controls.Code"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:issue="using:JitHub.Views.Controls.Issue"
    xmlns:local="using:JitHub.Views.Controls.Activity"
    xmlns:markdown="using:CommunityToolkit.Labs.WinUI.MarkdownTextBlock"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:usercontrol="using:JitHub.Views.Controls.Common"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid Style="{StaticResource ActivityContainer}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="4">
            <usercontrol:Avatar
                Login="{x:Bind ViewModel.Actor.Login, Mode=OneWay}"
                ShowLogin="True"
                Url="{x:Bind ViewModel.Actor.AvatarUrl, Mode=OneWay}" />
            <TextBlock VerticalAlignment="Center" Text="{x:Bind ViewModel.Action, Mode=OneWay}" />
            <TextBlock VerticalAlignment="Center" Text="a comment on an issue in" />
            <code:CodeButton
                VerticalAlignment="Center"
                Ref="{x:Bind ViewModel.Repo.DefaultBranch, Mode=OneWay}"
                Repo="{x:Bind ViewModel.Repo, Mode=OneWay}"
                Text="{x:Bind ViewModel.RepoName, Mode=OneWay}" />
            <TextBlock VerticalAlignment="Center" Text="{x:Bind ViewModel.CreatedAt, Converter={StaticResource TimeAgoConverter}, ConverterParameter='', Mode=OneWay}" />
        </StackPanel>
        <Grid Grid.Row="1" Padding="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <issue:IssueButton
                Grid.Row="0"
                Margin="0,4"
                Issue="{x:Bind ViewModel.Issue, Mode=OneWay}"
                Repo="{x:Bind ViewModel.Repo, Mode=OneWay}" />
            <ScrollViewer Grid.Row="1" MaxHeight="200">
                <markdown:MarkdownTextBlock Config="{x:Bind ViewModel.MarkdownConfig, Mode=OneWay}" Text="{x:Bind ViewModel.MarkdownText, Mode=OneWay}" />
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
