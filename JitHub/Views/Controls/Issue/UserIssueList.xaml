<UserControl
    x:Class="JitHub.Views.Controls.Issue.UserIssueList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JitHub.Views.Controls.Issue"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:issueviewmodels="using:JitHub.ViewModels.IssueViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.DataContext>
        <issueviewmodels:UserIssueListViewModel x:Name="ViewModel"/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="20"
                Text="{x:Bind ViewModel.Title, Mode=OneWay}"/>
            <controls:Segmented
                HorizontalAlignment="Right"
                Grid.Column="1"
                SelectionChanged="{x:Bind ViewModel.SegmentedSelectionChanged, Mode=OneWay}">
                <controls:SegmentedItem Content="Assigned" Tag="Assigned" IsSelected="True"/>
                <controls:SegmentedItem Content="Created" Tag="Created"/>
            </controls:Segmented>
        </Grid>
        <ListView
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.Issues, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding Title, Mode=OneWay}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
