<UserControl
    x:Class="JitHub.WinUI.Views.Controls.UserCommentBlock"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:JitHub.WinUI.Views.Controls.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:JitHub.WinUI.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:userviewmodels="using:JitHub.WinUI.ViewModels.UserViewModel"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Loaded="UserCommentBlock_Loaded"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" VerticalAlignment="Top">
            <common:Avatar
                Margin="{ThemeResource AppMargin0_0_8_0}"
                AutomationInstanceId="{x:Bind ViewModel.CommenterAvatarAutomationId, Mode=OneWay}"
                Login="{x:Bind ViewModel.AuthenticatedCommenterLogin, Mode=OneWay}"
                NavigationSource="comment_avatar"
                ShowLogin="False"
                Size="{x:Bind Size, Mode=OneWay}"
                Url="{x:Bind ViewModel.CommenterAvatarUrl, Mode=OneWay}" />
        </Grid>
        <common:CommentBlock Grid.Column="1">
            <common:CommentBlock.HeaderContent>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Column="0"
                        VerticalAlignment="Center"
                        Style="{StaticResource SectionSecondaryTextBlockStyle}"
                        Foreground="{ThemeResource AppInkMutedBrush}">
                        <Run FontWeight="{ThemeResource AppFontWeightSemibold}" Text="{x:Bind ViewModel.CommenterDisplayName, Mode=OneWay}" />
                        <Run Text="{x:Bind ViewModel.CreatedAt, Mode=OneWay}" />
                    </TextBlock>
                    <Grid Grid.Column="1" HorizontalAlignment="Right">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <common:EmojiPanelButton
                            AutomationInstanceId="{x:Bind ViewModel.HeaderReactionAutomationId, Mode=OneWay}"
                            ViewModel="{x:Bind ViewModel.EmojiPanelViewModel, Mode=OneWay}" />
                        <Button
                            x:Uid="ControlsUserCommentBlockXBindViewModelOverflowAutomation" Grid.Column="1"
                            Margin="{ThemeResource AppMargin0}"
                            Padding="{ThemeResource AppPadding4_2}"
                            AutomationProperties.AutomationId="{x:Bind ViewModel.OverflowAutomationId, Mode=OneWay}"
                            AutomationProperties.Name="Comment actions"
                            Style="{StaticResource AppToolbarButtonStyle}">
                            <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" Glyph="&#xE712;" />
                            <Button.Flyout>
                                <MenuFlyout Placement="BottomEdgeAlignedLeft">
                                    <MenuFlyoutItem
                                        x:Uid="ControlsUserCommentBlockXBindViewModelCopyLinkAutomation" x:Name="CopyLink"
                                        AutomationProperties.AutomationId="{x:Bind ViewModel.CopyLinkAutomationId, Mode=OneWay}"
                                        AutomationProperties.Name="Copy comment link"
                                        Command="{x:Bind ViewModel.CopyLinkCommand, Mode=OneWay}"
                                        Text="{x:Bind ViewModel.CopyLinkText, Mode=OneWay}" />
                                    <MenuFlyoutItem
                                        x:Uid="ControlsUserCommentBlockXBindViewModelQuoteReplyAutomati" x:Name="QuoteReply"
                                        AutomationProperties.AutomationId="{x:Bind ViewModel.QuoteReplyAutomationId, Mode=OneWay}"
                                        AutomationProperties.Name="Quote comment in reply"
                                        Command="{x:Bind ViewModel.QuoteReplyCommand, Mode=OneWay}"
                                        CommandParameter="{x:Bind ViewModel.QuoteReplyParameter, Mode=OneWay}"
                                        Text="{x:Bind ViewModel.QuoteReplyText, Mode=OneWay}" />
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </Grid>
                </Grid>
            </common:CommentBlock.HeaderContent>
            <common:CommentBlock.BodyContent>
                <Grid Padding="{ThemeResource AppPadding8}">
                    <StackPanel Spacing="{ThemeResource AppGap8}">
                        <muxc:InfoBar
                            AutomationProperties.AutomationId="LegacyCommentReactionError"
                            IsOpen="{x:Bind ViewModel.HasReactionError, Mode=TwoWay}"
                            Message="{x:Bind ViewModel.ReactionErrorText, Mode=OneWay}"
                            Severity="Error"
                            Style="{StaticResource AppErrorInfoBarStyle}" />
                        <common:MarkdownViewer
                            HostKind="Comment"
                            AutomationInstanceId="{x:Bind ViewModel.MarkdownAutomationId, Mode=OneWay}"
                            BaseUrl="https://github.com/"
                            DocumentSource="{x:Bind ViewModel.MarkdownSource, Mode=OneWay}"
                            Text="{x:Bind ViewModel.Body, Mode=OneWay}" />
                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="{ThemeResource AppGap8}"
                            Visibility="{x:Bind ViewModel.HasReaction, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                            <common:EmojiPanelButton
                                AutomationInstanceId="{x:Bind ViewModel.SummaryReactionAutomationId, Mode=OneWay}"
                                ViewModel="{x:Bind ViewModel.EmojiPanelViewModel, Mode=OneWay}" />
                            <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.ReactionWithUsers, Mode=OneWay}">
                                <muxc:ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="userviewmodels:ReactionWithUsers">
                                        <Grid>
                                            <common:EmojiButton
                                                AutomationInstanceId="{x:Bind AutomationInstanceId, Mode=OneWay}"
                                                Reaction="{x:Bind Type, Mode=OneWay}"
                                                ReactionCommand="{x:Bind ReactionCommand, Mode=OneWay}"
                                                ReactionCount="{x:Bind Users.Count, Mode=OneWay}"
                                                ShowReactionCount="True"
                                                Users="{x:Bind Users, Mode=OneWay}"
                                                Voted="{x:Bind Voted, Mode=OneWay}" />
                                        </Grid>
                                    </DataTemplate>
                                </muxc:ItemsRepeater.ItemTemplate>
                                <muxc:ItemsRepeater.Layout>
                                    <muxc:StackLayout Orientation="Horizontal" Spacing="{ThemeResource AppGap8}" />
                                </muxc:ItemsRepeater.Layout>
                            </muxc:ItemsRepeater>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </common:CommentBlock.BodyContent>
        </common:CommentBlock>
    </Grid>
</UserControl>




