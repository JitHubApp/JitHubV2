<UserControl
    x:Class="JitHub.WinUI.Views.Controls.Filter.TextFilterPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="104"
    d:DesignWidth="172">
    <Grid>
        <TextBox
            x:Uid="ControlsFilterTextFilterPresenterXBindAutomationIdModeOn" x:Name="FilterTextBox"
            Width="{ThemeResource AppDimension172}"
            MinWidth="{ThemeResource AppDimension172}"
            AutomationProperties.AutomationId="{x:Bind AutomationId, Mode=OneWay}"
            AutomationProperties.Name="{x:Bind Title, Mode=OneWay}"
            Header="{x:Bind Title, Mode=OneWay}"
            PlaceholderText="Type to filter"
            Style="{StaticResource AppTextBoxStyle}"
            TextChanged="OnTextChanged">
            <TextBox.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="{Binding Mode=OneWay}" />
                </DataTemplate>
            </TextBox.HeaderTemplate>
        </TextBox>
    </Grid>
</UserControl>
