<UserControl
    x:Class="JitHub.WinUI.Views.Controls.CodeViewer.Renderers.JsonPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:codeviewer="using:JitHub.WinUI.Views.Controls.CodeViewer"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource AppCanvasBrush}"
    d:DesignHeight="600"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Toolbar  -->
        <Border
            Grid.Row="0"
            Padding="8,6"
            Background="{ThemeResource AppSurfaceBrush}"
            BorderBrush="{ThemeResource AppOutlineBrush}"
            BorderThickness="0,0,0,1">
            <controls:Segmented
                x:Uid="JsonPreviewViewMode" x:Name="ViewModeSegmented"
                HorizontalAlignment="Left"
                AutomationProperties.AutomationId="JsonPreviewViewMode"
                AutomationProperties.Name="JSON view mode"
                SelectionChanged="ViewModeSegmented_SelectionChanged"
                SelectionMode="Single">
                <controls:SegmentedItem x:Uid="JsonPreviewViewModeRich" AutomationProperties.AutomationId="JsonPreviewViewMode_Rich" AutomationProperties.Name="Formatted JSON view" Content="Rich" />
                <controls:SegmentedItem x:Uid="JsonPreviewViewModePlain" AutomationProperties.AutomationId="JsonPreviewViewMode_Plain" AutomationProperties.Name="Plain JSON view" Content="Plain" />
            </controls:Segmented>
        </Border>

        <!--  Body: displays pretty-printed or raw JSON  -->
        <codeviewer:CodeEditorControl
            x:Name="Editor"
            Grid.Row="1"
            IsReadOnlyEditor="True"
            LanguageId="json" />

    </Grid>

</UserControl>
