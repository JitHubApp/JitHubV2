<UserControl
    x:Class="JitHub.WinUI.Views.Controls.CodeViewer.Renderers.HexPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:codeviewer="using:JitHub.WinUI.Views.Controls.CodeViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource AppCanvasBrush}"
    d:DesignHeight="600"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header: total byte count  -->
        <Border
            Grid.Row="0"
            Padding="12,6"
            Background="{ThemeResource AppSurfaceBrush}"
            BorderBrush="{ThemeResource AppOutlineBrush}"
            BorderThickness="0,0,0,1">
            <TextBlock
                x:Name="HeaderText"
                Foreground="{ThemeResource AppInkMutedBrush}"
                Style="{StaticResource SectionSecondaryTextBlockStyle}" />
        </Border>

        <!--  Hex dump  -->
        <codeviewer:CodeEditorControl
            x:Name="HexEditor"
            Grid.Row="1"
            IsReadOnlyEditor="True"
            LanguageId="text"
            ShowLineNumbers="False" />

    </Grid>

</UserControl>
