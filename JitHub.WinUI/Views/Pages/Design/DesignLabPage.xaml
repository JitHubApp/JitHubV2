<Page
    x:Class="JitHub.WinUI.Views.Pages.Design.DesignLabPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:appcontrols="using:JitHub.WinUI.Views.Controls.App"
    xmlns:common="using:JitHub.WinUI.Views.Controls.Common"
    xmlns:conversation="using:JitHub.WinUI.Views.Controls.PullRequest.Conversation"
    xmlns:helpers="using:JitHub.WinUI.Helpers"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource AppTransparentBrush}">
    <ScrollViewer AutomationProperties.AutomationId="DesignLabRoot">
        <StackPanel Padding="{ThemeResource AppPadding20}" Spacing="{ThemeResource AppGap20}">
            <appcontrols:AppSectionHeader
                Eyebrow="DESIGN SYSTEM"
                Title="WinUI Design Lab"
                Description="Deterministic states and reference surfaces for screenshot proof across themes and control families." />

            <appcontrols:AppSurfaceCard x:Name="ButtonsScenarioCard" AutomationProperties.AutomationId="ScenarioButtons">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioButtons"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader Title="Buttons" Description="Primary, quiet, stat, pressed, hover, and disabled states." />
                        <Grid ColumnSpacing="{ThemeResource AppGap12}" RowSpacing="{ThemeResource AppGap12}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Button Grid.Row="0" Grid.Column="0" AutomationProperties.AutomationId="DesignLabPrimaryButton" AutomationProperties.Name="Primary button sample" Content="Primary" Style="{StaticResource AppAccentButtonStyle}" />
                            <Button Grid.Row="0" Grid.Column="1" AutomationProperties.AutomationId="DesignLabHoverButton" AutomationProperties.Name="Pointer over button sample" helpers:PreviewStateHelper.VisualState="PointerOver" Content="Hover" Style="{StaticResource AppAccentButtonStyle}" />
                            <Button Grid.Row="0" Grid.Column="2" AutomationProperties.AutomationId="DesignLabPressedButton" AutomationProperties.Name="Pressed button sample" helpers:PreviewStateHelper.VisualState="Pressed" Content="Pressed" Style="{StaticResource AppAccentButtonStyle}" />
                            <Button Grid.Row="1" Grid.Column="0" AutomationProperties.AutomationId="DesignLabDisabledButton" AutomationProperties.Name="Disabled button sample" helpers:PreviewStateHelper.VisualState="Disabled" Content="Disabled" Style="{StaticResource AppAccentButtonStyle}" />
                            <Button Grid.Row="1" Grid.Column="1" AutomationProperties.AutomationId="DesignLabQuietButton" AutomationProperties.Name="Quiet button sample" Content="Quiet" Style="{StaticResource AppQuietButtonStyle}" />
                            <appcontrols:AppStatButton Grid.Row="1" Grid.Column="2" AutomationId="DesignLabButtonStat" AutomationName="Star statistic button sample" IconKind="Star" IconForeground="{ThemeResource AppInkMutedBrush}" SelectedIconForeground="{ThemeResource AppWarmAccentBrush}" IsSelected="True" ValueText="12.5k" />
                            <Button Grid.Row="0" Grid.Column="3" AutomationProperties.AutomationId="DesignLabDangerButton" AutomationProperties.Name="Close issue button sample" Content="Close issue" Style="{StaticResource AppDangerOutlineButtonStyle}" />
                            <Button Grid.Row="1" Grid.Column="3" AutomationProperties.AutomationId="DesignLabOverflowButton" AutomationProperties.Name="More actions button sample" Style="{StaticResource AppToolbarButtonStyle}">
                                <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" Glyph="&#xE711;" />
                            </Button>
                        </Grid>
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="InputsScenarioCard" AutomationProperties.AutomationId="ScenarioInputs">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioInputs"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader Title="Inputs" Description="Search, text, combo, date, and toggle surfaces." />
                        <AutoSuggestBox AutomationProperties.AutomationId="DesignLabSearchBox" AutomationProperties.Name="Search input sample" PlaceholderText="Search repositories" Style="{StaticResource AppAutoSuggestBoxStyle}" Text="flutter/flutter" />
                        <TextBox AutomationProperties.AutomationId="DesignLabTextBox" AutomationProperties.Name="Text input sample" Text="issue triage" Header="Text" Style="{StaticResource AppTextBoxStyle}" />
                        <ComboBox AutomationProperties.AutomationId="DesignLabStateComboBox" AutomationProperties.Name="State picker sample" SelectedIndex="0" Style="{StaticResource ComboBox}">
                            <x:String>Open</x:String>
                            <x:String>Closed</x:String>
                            <x:String>Merged</x:String>
                        </ComboBox>
                        <DatePicker AutomationProperties.AutomationId="DesignLabDatePicker" AutomationProperties.Name="Date picker sample" Style="{StaticResource DatePicker}" />
                        <ToggleSwitch AutomationProperties.AutomationId="DesignLabToggleSwitch" AutomationProperties.Name="Developer mode toggle sample" Header="Developer mode" IsOn="True" />
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="SegmentsScenarioCard" AutomationProperties.AutomationId="ScenarioSegments">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioSegments"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader Title="Segmented and action controls" Description="Repo filters, dropdown actions, and stat buttons with stable state geometry." />

                        <Grid ColumnSpacing="{ThemeResource AppGap20}" RowSpacing="{ThemeResource AppGap14}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Spacing="{ThemeResource AppGap8}">
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Repository filters" />
                                <appcontrols:AppSegmented
                                    Width="{ThemeResource AppDimension300}"
                                    AutomationProperties.AutomationId="DesignLabRepoSegmented"
                                    AutomationProperties.Name="Repository visibility sample"
                                    SelectedIndex="1"
                                    SelectionMode="Single">
                                    <appcontrols:AppSegmentedItem AutomationProperties.AutomationId="DesignLabRepoSegmented_Public" AutomationProperties.Name="Public repository sample" helpers:PreviewStateHelper.VisualState="PointerOver" Content="Public" />
                                    <appcontrols:AppSegmentedItem AutomationProperties.AutomationId="DesignLabRepoSegmented_Private" AutomationProperties.Name="Private repository sample" Content="Private" />
                                    <appcontrols:AppSegmentedItem AutomationProperties.AutomationId="DesignLabRepoSegmented_Forked" AutomationProperties.Name="Forked repository sample" Content="Forked" />
                                </appcontrols:AppSegmented>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Spacing="{ThemeResource AppGap8}">
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Issue filters" />
                                <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                                    <muxc:DropDownButton AutomationProperties.AutomationId="DesignLabIssueStateDropDown" AutomationProperties.Name="Issue state sample" Content="Open" Style="{StaticResource AppDropDownButtonStyle}">
                                        <muxc:DropDownButton.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem AutomationProperties.AutomationId="DesignLabIssueStateOpen" AutomationProperties.Name="Open issue state" Text="Open" />
                                                <MenuFlyoutItem AutomationProperties.AutomationId="DesignLabIssueStateClosed" AutomationProperties.Name="Closed issue state" Text="Closed" />
                                                <MenuFlyoutItem AutomationProperties.AutomationId="DesignLabIssueStateMerged" AutomationProperties.Name="Merged issue state" Text="Merged" />
                                            </MenuFlyout>
                                        </muxc:DropDownButton.Flyout>
                                    </muxc:DropDownButton>
                                    <Button AutomationProperties.AutomationId="DesignLabFilterButton" AutomationProperties.Name="Apply filter sample" Content="Filter" Style="{StaticResource AppOutlineButtonStyle}" />
                                    <Button AutomationProperties.AutomationId="DesignLabClearFilterButton" AutomationProperties.Name="Clear filter sample" Content="Clear" Style="{StaticResource AppQuietButtonStyle}" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Spacing="{ThemeResource AppGap8}">
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Repository actions" />
                                <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                                    <muxc:DropDownButton Width="{ThemeResource AppDimension160}" AutomationProperties.AutomationId="DesignLabBranchDropDown" AutomationProperties.Name="Branch picker sample" Content="main" Style="{StaticResource AppDropDownButtonStyle}" />
                                    <appcontrols:AppStatButton AutomationId="DesignLabActionsWatchStat" AutomationName="Watch repository action sample" IconKind="Watch" IconForeground="{ThemeResource AppInkMutedBrush}" SelectedIconForeground="{ThemeResource AppAccentBrush}" IsSelected="True" ValueText="2.8k" />
                                    <appcontrols:AppStatButton AutomationId="DesignLabActionsStarStat" AutomationName="Star repository action sample" IconKind="Star" IconForeground="{ThemeResource AppInkMutedBrush}" SelectedIconForeground="{ThemeResource AppWarmAccentBrush}" IsSelected="True" ValueText="170k" />
                                    <appcontrols:AppStatButton AutomationId="DesignLabActionsForkStat" AutomationName="Fork repository action sample" IconKind="Fork" IconForeground="{ThemeResource AppInkMutedBrush}" ValueText="28k" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="NavigationScenarioCard" AutomationProperties.AutomationId="ScenarioNavigation">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioNavigation"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader Title="Navigation and shell chrome" Description="Search rail, toolbar actions, and tabs rendered with the new shell language." />
                        <Border
                            Padding="{ThemeResource AppPadding16}"
                            Background="{ThemeResource AppCanvasRaisedBrush}"
                            BorderBrush="{ThemeResource AppOutlineBrush}"
                            BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                            CornerRadius="{ThemeResource AppRadiusLarge}">
                            <Grid RowSpacing="{ThemeResource AppGap12}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}" VerticalAlignment="Center">
                                    <Image Width="{ThemeResource AppDimension28}" Height="{ThemeResource AppDimension28}" VerticalAlignment="Center" Source="ms-appx:///Assets/JitHubLogoTitleBar.png" />
                                    <TextBlock VerticalAlignment="Center" Style="{StaticResource ListItemTitleTextBlockStyle}" Text="JitHub" />
                                </StackPanel>
                                <Border
                                    Grid.Column="1"
                                    Height="{ThemeResource AppDimension42}"
                                    Padding="{ThemeResource AppPadding14_0}"
                                    Background="{ThemeResource AppSurfaceBrush}"
                                    BorderBrush="{ThemeResource AppOutlineBrush}"
                                    BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                                    CornerRadius="{ThemeResource AppRadiusMedium}">
                                    <Grid ColumnSpacing="{ThemeResource AppGap10}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontFamily="{ThemeResource AppUiFontFamily}"
                                            Foreground="{ThemeResource AppInkBrush}"
                                            Text="flutter/flutter" />
                                        <FontIcon
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            FontFamily="{StaticResource SegoeFluentIcons}"
                                            Foreground="{ThemeResource AppInkMutedBrush}"
                                            Glyph="&#xE721;" />
                                    </Grid>
                                </Border>
                                <Button Grid.Column="2" AutomationProperties.AutomationId="DesignLabSettingsButton" AutomationProperties.Name="Settings toolbar sample" Style="{StaticResource AppToolbarButtonStyle}">
                                    <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" Glyph="&#xE712;" />
                                </Button>
                            </Grid>
                        </Border>
                        <muxc:TabView AutomationProperties.AutomationId="DesignLabTabView" AutomationProperties.Name="Workspace tabs sample" Style="{StaticResource JitTabViewStyle}">
                            <muxc:TabViewItem AutomationProperties.AutomationId="DesignLabTab_Home" AutomationProperties.Name="Home tab sample" Header="Home"><TextBlock Text="Home content" /></muxc:TabViewItem>
                            <muxc:TabViewItem AutomationProperties.AutomationId="DesignLabTab_Issues" AutomationProperties.Name="Issues tab sample" Header="Issues"><TextBlock Text="Issue content" /></muxc:TabViewItem>
                        </muxc:TabView>
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="SettingsScenarioCard" AutomationProperties.AutomationId="ScenarioSettings">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioSettings"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader Title="Settings reference" Description="App-owned settings cards and credit cards." />
                        <appcontrols:AppSettingsCard Header="Theme" Description="Set app theme to light, dark or follow Windows system theme.">
                            <appcontrols:AppSettingsCard.HeaderIcon>
                                <FontIcon Glyph="&#xE790;" />
                            </appcontrols:AppSettingsCard.HeaderIcon>
                            <appcontrols:AppSettingsCard.ActionContent>
                                <ComboBox Width="{ThemeResource AppDimension140}" AutomationProperties.AutomationId="DesignLabThemeComboBox" AutomationProperties.Name="Theme picker sample" SelectedIndex="0" Style="{StaticResource ComboBox}">
                                    <x:String>System</x:String>
                                    <x:String>Light</x:String>
                                    <x:String>Dark</x:String>
                                </ComboBox>
                            </appcontrols:AppSettingsCard.ActionContent>
                        </appcontrols:AppSettingsCard>
                        <appcontrols:AppSettingsCard Header="Sample build" Description="Design-only sample data">
                            <appcontrols:AppSettingsCard.HeaderIcon>
                                <BitmapIcon UriSource="ms-appx:///Assets/JitHubLogo.png" ShowAsMonochrome="False" />
                            </appcontrols:AppSettingsCard.HeaderIcon>
                            <appcontrols:AppSettingsCard.ActionContent>
                                <Button AutomationProperties.AutomationId="DesignLabViewSourceButton" AutomationProperties.Name="View source sample" Content="View Source" Style="{StaticResource AppAccentButtonStyle}" />
                            </appcontrols:AppSettingsCard.ActionContent>
                        </appcontrols:AppSettingsCard>
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="RepoScenarioCard" AutomationProperties.AutomationId="ScenarioRepoReference">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioRepoReference"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader Title="Repository reference" Description="Repo header, branch picker, and stat buttons." />
                        <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                            <ComboBox MinWidth="{ThemeResource AppDimension160}" AutomationProperties.AutomationId="DesignLabRepositoryBranchComboBox" AutomationProperties.Name="Repository branch sample" SelectedIndex="0" Style="{StaticResource ComboBox}">
                                <x:String>master</x:String>
                                <x:String>stable</x:String>
                            </ComboBox>
                            <appcontrols:AppStatButton AutomationId="DesignLabRepoWatchStat" AutomationName="Repository watch statistic sample" IconKind="Watch" IconForeground="{ThemeResource AppInkMutedBrush}" SelectedIconForeground="{ThemeResource AppAccentBrush}" IsSelected="True" ValueText="2.8k" />
                            <appcontrols:AppStatButton AutomationId="DesignLabRepoStarStat" AutomationName="Repository star statistic sample" IconKind="Star" IconForeground="{ThemeResource AppInkMutedBrush}" SelectedIconForeground="{ThemeResource AppWarmAccentBrush}" IsSelected="True" ValueText="170k" />
                            <appcontrols:AppStatButton AutomationId="DesignLabRepoForkStat" AutomationName="Repository fork statistic sample" IconKind="Fork" IconForeground="{ThemeResource AppInkMutedBrush}" ValueText="28k" />
                        </StackPanel>
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="ActivitiesScenarioCard" AutomationProperties.AutomationId="ScenarioActivities">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioActivities"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader
                            Title="Activity cards"
                            Description="Every documented GitHub Events API type rendered with typed details and in-app action chips." />
                        <Grid
                            HorizontalAlignment="Left"
                            ColumnSpacing="{ThemeResource AppGap16}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{ThemeResource AppGridLength760}" />
                                <ColumnDefinition Width="{ThemeResource AppGridLength380}" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="{ThemeResource AppGap8}">
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Roomy dashboard width" />
                                <ItemsControl
                                    ItemTemplateSelector="{StaticResource GitHubActivityCardTemplateSelector}"
                                    ItemsSource="{x:Bind ActivityCards, Mode=OneWay}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="{ThemeResource AppGap8}">
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Dense sidebar width" />
                                <ItemsControl
                                    ItemTemplateSelector="{StaticResource GitHubActivityCardTemplateSelector}"
                                    ItemsSource="{x:Bind ActivityCards, Mode=OneWay}" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="ConversationScenarioCard" AutomationProperties.AutomationId="ScenarioConversation">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioConversation"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader Title="Conversation reference" Description="Comment surfaces, labels, and reactions." />
                        <common:MarkdownForm
                            AutomationProperties.AutomationId="DesignLabMarkdownComposer"
                            AutomationInstanceId="DesignLabMarkdownComposer"
                            EditorHeight="120"
                            HorizontalAlignment="Left"
                            Width="{ThemeResource AppDimension920}"
                            Text="Thanks for the detailed report. I can reproduce this on the current build.">
                            <common:MarkdownForm.ActionContent>
                                <StackPanel Style="{StaticResource ActionPanel}">
                                    <Button AutomationProperties.AutomationId="DesignLabCloseIssueAction" AutomationProperties.Name="Close issue action sample" Content="Close issue" Style="{StaticResource AppDangerOutlineButtonStyle}" />
                                    <Button AutomationProperties.AutomationId="DesignLabCommentAction" AutomationProperties.Name="Comment action sample" Content="Comment" Style="{StaticResource AppAccentButtonStyle}" />
                                </StackPanel>
                            </common:MarkdownForm.ActionContent>
                        </common:MarkdownForm>
                        <common:CommentBlock>
                            <common:CommentBlock.HeaderContent>
                                <Grid>
                                    <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}">
                                        <Run FontWeight="{ThemeResource AppFontWeightSemibold}" Text="nerocui" />
                                        <Run Text=" commented 3h ago" />
                                    </TextBlock>
                                </Grid>
                            </common:CommentBlock.HeaderContent>
                            <common:CommentBlock.BodyContent>
                                <StackPanel Padding="{ThemeResource AppPadding12}" Spacing="{ThemeResource AppGap12}">
                                    <TextBlock Text="This is a reference conversation block for screenshot proof." />
                                    <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap6}">
                                        <common:RepoLabel Label="{x:Bind BugLabel, Mode=OneWay}" />
                                        <common:RepoLabel Label="{x:Bind AccessibilityLabel, Mode=OneWay}" />
                                        <common:RepoLabel Label="{x:Bind NeedsTriageLabel, Mode=OneWay}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                                        <Border
                                            Height="{ThemeResource AppDimension24}"
                                            MinWidth="{ThemeResource AppDimension48}"
                                            Padding="{ThemeResource AppPadding8_0}"
                                            Background="{ThemeResource AppSuccessBrush}"
                                            CornerRadius="{ThemeResource AppRadiusSmall}">
                                            <TextBlock Style="{StaticResource AppStatusPillTextBlockStyle}" Text="Open" />
                                        </Border>
                                        <Border
                                            Height="{ThemeResource AppDimension24}"
                                            MinWidth="{ThemeResource AppDimension56}"
                                            Padding="{ThemeResource AppPadding8_0}"
                                            Background="{ThemeResource AppDangerBrush}"
                                            CornerRadius="{ThemeResource AppRadiusSmall}">
                                            <TextBlock Style="{StaticResource AppStatusPillTextBlockStyle}" Text="Closed" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                                        <common:EmojiButton AutomationInstanceId="DesignLabReactionSummary" Reaction="Plus1" ReactionCount="12" ShowReactionCount="True" Voted="True" />
                                        <common:EmojiButton AutomationInstanceId="DesignLabReactionSummary" Reaction="Heart" ReactionCount="4" ShowReactionCount="True" />
                                        <common:EmojiButton AutomationInstanceId="DesignLabReactionSummary" Reaction="Hooray" />
                                        <common:EmojiButton AutomationInstanceId="DesignLabReactionSummary" Reaction="Eyes" />
                                        <common:EmojiPanelButton AutomationInstanceId="DesignLabReactionLauncher" ViewModel="{x:Bind DemoEmojiPanelViewModel}" />
                                    </StackPanel>
                                </StackPanel>
                            </common:CommentBlock.BodyContent>
                        </common:CommentBlock>
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="PullRequestTimelineScenarioCard" AutomationProperties.AutomationId="ScenarioPullRequestTimeline">
                <appcontrols:AppSurfaceCard.Child>
                    <StackPanel
                        AutomationProperties.AutomationId="ScenarioPullRequestTimeline"
                        Spacing="{ThemeResource AppGap16}">
                        <appcontrols:AppSectionHeader
                            Title="Pull request timeline"
                            Description="Commit and PR update rows rendered as inline timeline sentences, using the same link/tone tokens as activity cards." />
                        <Grid
                            HorizontalAlignment="Left"
                            ColumnSpacing="{ThemeResource AppGap16}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{ThemeResource AppGridLength760}" />
                                <ColumnDefinition Width="{ThemeResource AppGridLength380}" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="{ThemeResource AppGap8}">
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Roomy conversation width" />
                                <ItemsControl ItemsSource="{x:Bind PullRequestTimelineItems, Mode=OneWay}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <conversation:PullRequestTimelineItem Node="{Binding}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Spacing="{ThemeResource AppGap8}" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="{ThemeResource AppGap8}">
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Dense conversation width" />
                                <ItemsControl ItemsSource="{x:Bind PullRequestTimelineItems, Mode=OneWay}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <conversation:PullRequestTimelineItem Node="{Binding}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Spacing="{ThemeResource AppGap8}" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>

            <appcontrols:AppSurfaceCard x:Name="EmptyScenarioCard" AutomationProperties.AutomationId="ScenarioEmptyState">
                <appcontrols:AppSurfaceCard.Child>
                    <common:EmptyStateView
                        AutomationProperties.AutomationId="ScenarioEmptyState"
                        Title="Nothing here yet"
                        Description="Use this surface to verify quiet empty states in both themes." />
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>
        </StackPanel>
    </ScrollViewer>
</Page>

