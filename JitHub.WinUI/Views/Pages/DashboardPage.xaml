<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JitHub.WinUI.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:JitHub.WinUI.Views.Controls.Common"
    xmlns:behaviors="using:JitHub.WinUI.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:appcontrols="using:JitHub.WinUI.Views.Controls.App"
    xmlns:repo="using:JitHub.WinUI.Views.Controls"
    xmlns:viewmodels="using:JitHub.WinUI.ViewModels.Pages"
    x:DataType="viewmodels:DashboardPageViewModel"
    Background="Transparent"
    mc:Ignorable="d">
    <Grid Padding="{ThemeResource AppPagePadding}">
        <Grid HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="308" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <repo:RepoSideBar Grid.Column="0" />

            <appcontrols:AppSurfaceCard
                Grid.Column="2"
                CardPadding="20">
                <appcontrols:AppSurfaceCard.Child>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <appcontrols:AppSectionHeader
                            Margin="0,0,0,16"
                            Eyebrow="RECENT"
                            Title="All Activities"
                            Description="The latest activity across the repositories you care about." />

                        <ProgressRing
                            Grid.Row="1"
                            Width="50"
                            Height="50"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsActive="{Binding IsActivityLoading}"
                            Visibility="{Binding IsActivityLoading, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <controls:EmptyStateView
                            Grid.Row="1"
                            Description="{Binding RecentActivityEmptyStateDescription}"
                            Title="{Binding RecentActivityEmptyStateTitle}"
                            Visibility="{Binding IsActivityEmptyStateVisible, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <ListView
                            Grid.Row="1"
                            MaxWidth="960"
                            HorizontalAlignment="Center"
                            IsItemClickEnabled="False"
                            ItemTemplateSelector="{StaticResource GitHubActivityCardTemplateSelector}"
                            ItemsSource="{Binding RecentActivity}"
                            behaviors:IncrementalLoadingBehavior.IsEnabled="True"
                            behaviors:IncrementalLoadingBehavior.PageSize="20"
                            behaviors:IncrementalLoadingBehavior.Source="{Binding RecentActivity}"
                            behaviors:IncrementalLoadingBehavior.Threshold="1200"
                            SelectionMode="None"
                            Visibility="{Binding AreActivityItemsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0" />
                                    <Setter Property="Background" Value="Transparent" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>

                        <ProgressBar
                            Grid.Row="1"
                            Width="220"
                            Height="2"
                            Margin="0,0,0,4"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Background="{ThemeResource AppSurfaceSubtleBrush}"
                            Foreground="{ThemeResource AppAccentBrush}"
                            IsIndeterminate="True"
                            Visibility="{Binding IsActivityIncrementalLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </Grid>
                </appcontrols:AppSurfaceCard.Child>
            </appcontrols:AppSurfaceCard>
        </Grid>
    </Grid>
</Page>
