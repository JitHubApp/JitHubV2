<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="JitHub.WinUI.Views.Pages.RepoPullRequestPage"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:app="using:JitHub.WinUI.Views.Controls.App"
    xmlns:commit="using:JitHub.WinUI.Views.Controls.Commit"
    xmlns:common="using:JitHub.WinUI.Views.Controls.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:github="using:JitHub.Models.GitHub"
    xmlns:labs="using:CommunityToolkit.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:viewmodels="using:JitHub.WinUI.ViewModels.Pages"
    Background="{ThemeResource AppTransparentBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="RepoPullRequestsPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource AppSurfaceSubtleBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource AppHairlineBrush}" />
            <Setter Property="BorderThickness" Value="{ThemeResource AppHairlineBorderThickness}" />
            <Setter Property="CornerRadius" Value="{ThemeResource AppRadiusSmall}" />
        </Style>

        <ItemsPanelTemplate x:Key="RepoPullRequestVirtualizingItemsPanel">
            <ItemsStackPanel CacheLength="8" />
        </ItemsPanelTemplate>

        <DataTemplate x:Key="RepoPullRequestListItemTemplate" x:DataType="github:GitHubPullRequest">
            <Grid
                MinHeight="{ThemeResource AppDimension52}"
                AutomationProperties.AutomationId="{x:Bind AutomationId, Mode=OneWay}"
                AutomationProperties.Name="{x:Bind AutomationName, Mode=OneWay}"
                Background="{ThemeResource AppTransparentBrush}"
                RowSpacing="{ThemeResource AppGap5}"
                PointerEntered="PullRequestListItem_PointerEntered"
                PointerPressed="PullRequestListItem_PointerPressed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    FontFamily="{ThemeResource AppUiFontFamily}"
                    FontSize="{ThemeResource AppFontSize13}"
                    FontWeight="{ThemeResource AppFontWeightSemibold}"
                    Foreground="{ThemeResource AppInkBrush}"
                    MaxLines="2"
                    Text="{x:Bind Title, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="WrapWholeWords" />
                <Grid Grid.Row="1" ColumnSpacing="{ThemeResource AppGap8}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{ThemeResource AppMonoFontFamily}"
                        Foreground="{ThemeResource AppAccentBrush}"
                        Style="{StaticResource AppInlineMetadataTextBlockStyle}">
                        <Run Text="#" /><Run Text="{x:Bind Number, Mode=OneWay}" />
                    </TextBlock>
                    <common:Avatar
                        Grid.Column="1"
                        AutomationInstanceId="{x:Bind AutomationId, Mode=OneWay}"
                        ContainerCornerRadius="{ThemeResource AppRadiusSmall}"
                        DisplayName="{x:Bind User.Login, Mode=OneWay}"
                        LabelStyle="{StaticResource AppInlineIdentityTextBlockStyle}"
                        Login="{x:Bind User.Login, Mode=OneWay}"
                        MaxWidth="{ThemeResource AppDimension240}"
                        NavigationSource="pull_request_list_author"
                        ShowLogin="True"
                        Size="SMALL"
                        VerticalAlignment="Center"
                        Url="{x:Bind User.AvatarUrl, Mode=OneWay}" />
                </Grid>
                <Grid Grid.Row="2" ColumnSpacing="{ThemeResource AppGap8}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        FontFamily="{ThemeResource AppMonoFontFamily}"
                        Foreground="{ThemeResource AppInkMutedBrush}"
                        Style="{StaticResource AppInlineMetadataTextBlockStyle}"
                        Text="{x:Bind Head.GitRef, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis" />
                    <TextBlock
                        Grid.Column="1"
                        Style="{StaticResource AppInlineMetadataTextBlockStyle}"
                        Text="{x:Bind UpdatedAt, Converter={StaticResource TimeAgoConverter}, Mode=OneWay}"
                        TextAlignment="Right" />
                </Grid>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="RepoPullRequestCommentTemplate" x:DataType="github:GitHubIssueComment">
            <Border
                Margin="{ThemeResource AppMargin0_0_0_12}"
                Padding="{ThemeResource AppPadding14}"
                Background="{ThemeResource AppCanvasInsetBrush}"
                BorderBrush="{ThemeResource AppHairlineBrush}"
                BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                CornerRadius="{ThemeResource AppRadiusSmall}">
                <Grid RowSpacing="{ThemeResource AppGap12}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" MinHeight="{ThemeResource AppReactionIconButtonSize}" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="{ThemeResource AppGap10}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{ThemeResource AppGridLength32}" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <common:Avatar
                            VerticalAlignment="Center"
                            AutomationInstanceId="{x:Bind AvatarAutomationId, Mode=OneWay}"
                            DisplayName="{x:Bind AuthorDisplayName, Mode=OneWay}"
                            Login="{x:Bind AuthorProfileLogin, Mode=OneWay}"
                            NavigationSource="pull_request_comment"
                            ShowLogin="False"
                            Size="SMALL"
                            Url="{x:Bind AuthorAvatarUrl, Mode=OneWay}" />
                        <TextBlock
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            FontFamily="{ThemeResource AppUiFontFamily}"
                            FontSize="{ThemeResource AppFontSize13}"
                            FontWeight="{ThemeResource AppFontWeightSemibold}"
                            Foreground="{ThemeResource AppInkBrush}"
                            Text="{x:Bind AuthorDisplayName, Mode=OneWay}" />
                        <TextBlock
                            Grid.Column="2"
                            VerticalAlignment="Center"
                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                            Text="{x:Bind CreatedAt, Converter={StaticResource TimeAgoConverter}, Mode=OneWay}" />
                    </Grid>
                    <common:MarkdownViewer
                        HostKind="Comment"
                        AutomationInstanceId="{x:Bind MarkdownAutomationId, Mode=OneWay}"
                        Grid.Row="1"
                        BaseUrl="https://github.com/"
                        DocumentSource="{x:Bind MarkdownSource, Mode=OneWay}"
                        Text="{x:Bind Body, Mode=OneWay}" />
                    <common:CommentInteractionBar
                        Grid.Row="2"
                        AutomationInstanceId="{x:Bind MarkdownAutomationId, Mode=OneWay}"
                        ActionRequested="CommentInteractionBar_ActionRequested"
                        AuthorLogin="{x:Bind User.Login, Mode=OneWay}"
                        Body="{x:Bind Body, Mode=OneWay}"
                        CanModerate="{x:Bind CanViewerModerate, Mode=OneWay}"
                        CanReact="{x:Bind CanViewerReact, Mode=OneWay}"
                        CanReply="{x:Bind CanViewerReply, Mode=OneWay}"
                        HtmlUrl="{x:Bind HtmlUrl, Mode=OneWay}"
                        IsMinimized="{x:Bind IsMinimized, Mode=OneWay}"
                        IsPinned="{x:Bind IsPinned, Mode=OneWay}"
                        NodeId="{x:Bind NodeId, Mode=OneWay}"
                        TargetId="{x:Bind Id, Mode=OneWay}"
                        TargetKind="PullRequestComment"
                        Reactions="{x:Bind Reactions, Mode=OneWay}"
                        ViewerLogin="{x:Bind ViewerLogin, Mode=OneWay}" />
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="RepoPullRequestCommitTemplate" x:DataType="github:GitHubCommit">
            <Border
                Margin="{ThemeResource AppMargin0_0_0_10}"
                Padding="{ThemeResource AppPadding12}"
                Background="{ThemeResource AppCanvasInsetBrush}"
                BorderBrush="{ThemeResource AppHairlineBrush}"
                BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                CornerRadius="{ThemeResource AppRadiusSmall}">
                <Grid ColumnSpacing="{ThemeResource AppGap12}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <common:Avatar
                        VerticalAlignment="Center"
                        AutomationInstanceId="{x:Bind ShortSha, Mode=OneWay}"
                        DisplayName="{x:Bind AuthorDisplayName, Mode=OneWay}"
                        Login="{x:Bind AuthorProfileLogin, Mode=OneWay}"
                        NavigationSource="pull_request_commit_author"
                        ShowLogin="False"
                        Size="SMALL"
                        Url="{x:Bind AuthorAvatarUrl, Mode=OneWay}" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="{ThemeResource AppGap4}">
                        <TextBlock
                            FontFamily="{ThemeResource AppUiFontFamily}"
                            FontSize="{ThemeResource AppFontSize13}"
                            FontWeight="{ThemeResource AppFontWeightSemibold}"
                            Foreground="{ThemeResource AppInkBrush}"
                            MaxLines="2"
                            Text="{x:Bind SummaryMessage, Mode=OneWay}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="WrapWholeWords" />
                        <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="{x:Bind AuthorDisplayName, Mode=OneWay}" />
                    </StackPanel>
                    <TextBlock
                        Grid.Column="2"
                        VerticalAlignment="Center"
                        FontFamily="{ThemeResource AppMonoFontFamily}"
                        FontSize="{ThemeResource AppFontSize11}"
                        Foreground="{ThemeResource AppAccentBrush}"
                        Text="{x:Bind ShortSha, Mode=OneWay}" />
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="RepoPullRequestTimelineTemplate" x:DataType="github:GitHubIssueEvent">
            <Grid
                Margin="{ThemeResource AppMargin0_0_0_12}"
                ColumnSpacing="{ThemeResource AppGap10}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{ThemeResource AppGridLength24}" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                    <common:Avatar
                        AutomationInstanceId="{x:Bind ActorAutomationId, Mode=OneWay}"
                        DisplayName="{x:Bind ActorDisplayName, Mode=OneWay}"
                        Login="{x:Bind ActorProfileLogin, Mode=OneWay}"
                        NavigationSource="pull_request_timeline_actor"
                        ShowLogin="False"
                        Size="SMALL"
                        Url="{x:Bind ActorAvatarUrl, Mode=OneWay}" />
                <StackPanel Grid.Column="1" Spacing="{ThemeResource AppGap3}">
                    <TextBlock
                        FontFamily="{ThemeResource AppUiFontFamily}"
                        FontSize="{ThemeResource AppFontSize13}"
                        Foreground="{ThemeResource AppInkBrush}"
                        Text="{x:Bind Summary, Mode=OneWay}"
                        TextWrapping="WrapWholeWords" />
                    <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="{x:Bind MetaText, Mode=OneWay}" />
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate
            x:Key="RepoPullRequestReviewTemplate"
            x:DataType="viewmodels:IPullRequestReviewItem">
            <Border
                Margin="{ThemeResource AppMargin0_0_0_12}"
                Padding="{ThemeResource AppPadding14}"
                Background="{ThemeResource AppCanvasInsetBrush}"
                BorderBrush="{ThemeResource AppHairlineBrush}"
                BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                CornerRadius="{ThemeResource AppRadiusSmall}">
                <StackPanel Spacing="{ThemeResource AppGap12}">
                    <Grid ColumnSpacing="{ThemeResource AppGap10}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                            <common:Avatar
                                VerticalAlignment="Center"
                                AutomationInstanceId="{x:Bind AutomationId, Mode=OneWay}"
                                DisplayName="{x:Bind ReviewerLogin, Mode=OneWay}"
                                Login="{x:Bind ReviewerProfileLogin, Mode=OneWay}"
                                NavigationSource="pull_request_reviewer"
                                ShowLogin="False"
                                Size="SMALL"
                                Url="{x:Bind ReviewerAvatarUrl, Mode=OneWay}" />
                            <TextBlock
                                VerticalAlignment="Center"
                                FontFamily="{ThemeResource AppUiFontFamily}"
                                FontSize="{ThemeResource AppFontSize13}"
                                FontWeight="{ThemeResource AppFontWeightSemibold}"
                                Foreground="{ThemeResource AppInkBrush}"
                                Text="{x:Bind ReviewerLogin, Mode=OneWay}" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                Text="{x:Bind StateText, Mode=OneWay}" />
                        </StackPanel>
                        <TextBlock
                            Grid.Column="1"
                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                            Text="{x:Bind SubmittedAtText, Mode=OneWay}" />
                    </Grid>
                    <common:MarkdownViewer
                        HostKind="Comment"
                        AutomationInstanceId="{x:Bind AutomationId, Mode=OneWay}"
                        BaseUrl="https://github.com/"
                        DocumentSource="{x:Bind MarkdownSource, Mode=OneWay}"
                        Text="{x:Bind BodyText, Mode=OneWay}" />
                    <ItemsControl ItemsSource="{x:Bind Threads, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="viewmodels:IPullRequestReviewThreadItem">
                                <Border
                                    Margin="{ThemeResource AppMargin0_0_0_10}"
                                    Padding="{ThemeResource AppPadding12}"
                                    Background="{ThemeResource AppSurfaceSubtleBrush}"
                                    BorderBrush="{ThemeResource AppHairlineBrush}"
                                    BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                                    CornerRadius="{ThemeResource AppRadiusSmall}">
                                    <StackPanel Spacing="{ThemeResource AppGap10}">
                                        <Grid ColumnSpacing="{ThemeResource AppGap8}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                FontFamily="{ThemeResource AppMonoFontFamily}"
                                                FontSize="{ThemeResource AppFontSize11}"
                                                Foreground="{ThemeResource AppAccentBrush}"
                                                Text="{x:Bind PathDisplayText, Mode=OneWay}"
                                                TextTrimming="CharacterEllipsis" />
                                            <TextBlock
                                                Grid.Column="1"
                                                Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                                Text="{x:Bind CreatedAtText, Mode=OneWay}" />
                                        </Grid>
                                        <TextBlock
                                            FontFamily="{ThemeResource AppMonoFontFamily}"
                                            FontSize="{ThemeResource AppFontSize11}"
                                            Foreground="{ThemeResource AppInkMutedBrush}"
                                            MaxLines="4"
                                            Text="{x:Bind DiffHunkText, Mode=OneWay}"
                                            TextWrapping="NoWrap" />
                                        <common:MarkdownViewer
                                            HostKind="Comment"
                                            AutomationInstanceId="{x:Bind AutomationId, Mode=OneWay}"
                                            BaseUrl="https://github.com/"
                                            DocumentSource="{x:Bind MarkdownSource, Mode=OneWay}"
                                            SurfaceColorToken="AppSurfaceSubtle"
                                            Text="{x:Bind CommentBody, Mode=OneWay}" />
                                        <common:CommentInteractionBar
                                            AutomationInstanceId="{x:Bind AutomationId, Mode=OneWay}"
                                            ActionRequested="CommentInteractionBar_ActionRequested"
                                            AuthorLogin="{x:Bind CommentAuthorLogin, Mode=OneWay}"
                                            Body="{x:Bind CommentBody, Mode=OneWay}"
                                            CanModerate="{x:Bind CanModerate, Mode=OneWay}"
                                            CanReact="{x:Bind CanReact, Mode=OneWay}"
                                            CanReply="{x:Bind CanReply, Mode=OneWay}"
                                            HtmlUrl="{x:Bind CommentHtmlUrl, Mode=OneWay}"
                                            IsMinimized="{x:Bind IsMinimized, Mode=OneWay}"
                                            NodeId="{x:Bind CommentNodeId, Mode=OneWay}"
                                            Reactions="{x:Bind Reactions, Mode=OneWay}"
                                            TargetId="{x:Bind CommentId, Mode=OneWay}"
                                            TargetKind="PullRequestReviewComment"
                                            ViewerLogin="{x:Bind ViewerLogin, Mode=OneWay}" />
                                        <ItemsControl ItemsSource="{x:Bind Replies, Mode=OneWay}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border
                                                        Margin="{ThemeResource AppMargin18_2_0_2}"
                                                        Padding="{ThemeResource AppPadding12_8_0_8}"
                                                        BorderBrush="{ThemeResource AppHairlineBrush}"
                                                        BorderThickness="{ThemeResource AppLeftHairlineBorderThickness}">
                                                        <StackPanel Spacing="{ThemeResource AppGap8}">
                                                            <Grid ColumnSpacing="{ThemeResource AppGap8}">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock
                                                                    FontSize="{ThemeResource AppFontSize12}"
                                                                    FontWeight="{ThemeResource AppFontWeightSemibold}"
                                                                    Foreground="{ThemeResource AppInkBrush}"
                                                                    Text="{Binding UserLogin}" />
                                                                <TextBlock
                                                                    Grid.Column="1"
                                                                    Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                                                    Text="{Binding CreatedAtText}" />
                                                            </Grid>
                                                            <common:MarkdownViewer
                                                                HostKind="Comment"
                                                                AutomationInstanceId="{Binding AutomationId}"
                                                                BaseUrl="https://github.com/"
                                                                DocumentSource="{Binding MarkdownSource}"
                                                                SurfaceColorToken="AppSurfaceSubtle"
                                                                Text="{Binding Body}" />
                                                            <common:CommentInteractionBar
                                                                AutomationInstanceId="{Binding AutomationId}"
                                                                ActionRequested="CommentInteractionBar_ActionRequested"
                                                                AuthorLogin="{Binding AuthorLogin}"
                                                                Body="{Binding Body}"
                                                                CanModerate="{Binding CanModerate}"
                                                                CanReact="{Binding CanReact}"
                                                                CanReply="{Binding CanReply}"
                                                                HtmlUrl="{Binding HtmlUrl}"
                                                                IsMinimized="{Binding IsMinimized}"
                                                                NodeId="{Binding NodeId}"
                                                                Reactions="{Binding Reactions}"
                                                                TargetId="{Binding Id}"
                                                                TargetKind="PullRequestReviewComment"
                                                                ViewerLogin="{Binding ViewerLogin}" />
                                                        </StackPanel>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <common:MarkdownForm
                                            AutomationInstanceId="{x:Bind ReplyFormAutomationId, Mode=OneWay}"
                                            DocumentSource="{x:Bind ReplyMarkdownSource, Mode=OneWay}"
                                            EditorHeight="160"
                                            Text="{x:Bind ReplyText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                            <common:MarkdownForm.ActionContent>
                                                <Button
                                                    x:Uid="PagesRepoPullRequestPageBindingReplyAutomationId" MinHeight="{ThemeResource AppDimension32}"
                                                    MinWidth="{ThemeResource AppDimension72}"
                                                    HorizontalAlignment="Right"
                                                    AutomationProperties.AutomationId="{x:Bind ReplyAutomationId, Mode=OneWay}"
                                                    AutomationProperties.Name="Reply to review comment"
                                                    Click="ReviewReplyButton_Click"
                                                    Content="{x:Bind ReplyButtonText, Mode=OneWay}"
                                                    IsEnabled="{x:Bind IsReplyEnabled, Mode=OneWay}"
                                                    Style="{StaticResource AppOutlineButtonStyle}" />
                                            </common:MarkdownForm.ActionContent>
                                        </common:MarkdownForm>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="RepoPullRequestLabelTemplate" x:DataType="github:GitHubLabel">
            <Border
                Margin="{ThemeResource AppMargin0_0_6_6}"
                Padding="{ThemeResource AppPadding8_4}"
                Background="{ThemeResource AppCanvasInsetBrush}"
                BorderBrush="{ThemeResource AppHairlineBrush}"
                BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                CornerRadius="{ThemeResource AppRadiusSmall}">
                <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap6}">
                    <Ellipse
                        Width="{ThemeResource AppDimension7}"
                        Height="{ThemeResource AppDimension7}"
                        VerticalAlignment="Center"
                        Fill="{x:Bind Color, Converter={StaticResource HexColorToColorStringConverter}, Mode=OneWay}" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontFamily="{ThemeResource AppMonoFontFamily}"
                        FontSize="{ThemeResource AppFontSize11}"
                        Foreground="{ThemeResource AppInkBrush}"
                        Text="{x:Bind Name, Mode=OneWay}" />
                </StackPanel>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="RepoPullRequestReviewerActorTemplate" x:DataType="github:GitHubActor">
            <Grid
                MinHeight="{ThemeResource AppDimension34}"
                Margin="{ThemeResource AppMargin0_0_0_6}"
                ColumnSpacing="{ThemeResource AppGap8}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{ThemeResource AppGridLength28}" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <common:Avatar
                    VerticalAlignment="Center"
                    AutomationInstanceId="{x:Bind AutomationId, Mode=OneWay}"
                    Login="{x:Bind Login, Mode=OneWay}"
                    NavigationSource="pull_request_requested_reviewer"
                    ShowLogin="False"
                    Size="SMALL"
                    Url="{x:Bind AvatarUrl, Mode=OneWay}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Style="{StaticResource SectionSecondaryTextBlockStyle}"
                    Text="{x:Bind Login, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="RepoPullRequestAssigneeActorTemplate" x:DataType="github:GitHubActor">
            <Grid
                MinHeight="{ThemeResource AppDimension34}"
                Margin="{ThemeResource AppMargin0_0_0_6}"
                ColumnSpacing="{ThemeResource AppGap8}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="{ThemeResource AppGridLength28}" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <common:Avatar
                    VerticalAlignment="Center"
                    AutomationInstanceId="{x:Bind AutomationId, Mode=OneWay}"
                    Login="{x:Bind Login, Mode=OneWay}"
                    NavigationSource="pull_request_assignee"
                    ShowLogin="False"
                    Size="SMALL"
                    Url="{x:Bind AvatarUrl, Mode=OneWay}" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Style="{StaticResource SectionSecondaryTextBlockStyle}"
                    Text="{x:Bind Login, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid
        x:Uid="PagesRepoPullRequestPageRepoPullRequestsPageRoot" x:Name="PerformanceRoot"
        AutomationProperties.AccessibilityView="Control"
        AutomationProperties.AutomationId="RepoPullRequestsPageRoot"
        AutomationProperties.Name="Repository pull requests"
        Padding="{ThemeResource AppPagePadding}">
        <app:AdaptiveWorkspace
            x:Name="PullRequestsWorkspace"
            AutomationProperties.AutomationId="RepoPullRequestsAdaptiveWorkspace"
            AutomationIdPrefix="RepoPullRequests"
            LeadingPaneLabel="Pull requests"
            LeadingPaneWidth="336"
            MediumBreakpoint="760"
            ModeChanged="PullRequestsWorkspace_ModeChanged"
            NarrowBreakpoint="560"
            ShowPaneButtons="False"
            TrailingPaneLabel="Inspector"
            TrailingPaneWidth="260"
            WideBreakpoint="1260">
            <app:AdaptiveWorkspace.LeadingPane>
                <Border
                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsListHost" x:Name="PullRequestListHost"
                    AutomationProperties.AccessibilityView="Control"
                    AutomationProperties.AutomationId="RepoPullRequestsListHost"
                    AutomationProperties.Name="Pull request list pane"
                    Padding="{ThemeResource AppPadding10}"
                    Style="{StaticResource RepoPullRequestsPanelStyle}">
                    <Grid RowSpacing="{ThemeResource AppGap10}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid RowSpacing="{ThemeResource AppGap8}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid ColumnSpacing="{ThemeResource AppGap0}" Style="{StaticResource AppWorkspaceHeaderStyle}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsCloseListPaneBut" x:Name="RepoPullRequestsCloseListPaneButton"
                                    Width="{ThemeResource AppDimension36}"
                                    Height="{ThemeResource AppDimension36}"
                                    Margin="{ThemeResource AppMargin0_0_8_0}"
                                    Padding="{ThemeResource AppPadding0}"
                                    VerticalAlignment="Top"
                                    AutomationProperties.AutomationId="RepoPullRequestsCloseListPaneButton"
                                    AutomationProperties.Name="Close pull request list"
                                    Click="CloseWorkspaceDrawerButton_Click"
                                    Style="{StaticResource AppToolbarButtonStyle}"
                                    ToolTipService.ToolTip="Close pull requests"
                                    Visibility="Collapsed">
                                    <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize14}" Glyph="&#xE8FD;" />
                                </Button>
                                <app:AppSegmented
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsStateSegmented" x:Name="PullRequestStateSegmented"
                                    Grid.Column="1"
                                    AutomationProperties.AutomationId="RepoPullRequestsStateSegmented"
                                    AutomationProperties.Name="Pull request state filter"
                                    SelectedIndex="0"
                                    SelectionChanged="PullRequestStateSegmented_SelectionChanged">
                                    <app:AppSegmentedItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsStateOpen" AutomationProperties.AutomationId="RepoPullRequestsState_Open" AutomationProperties.Name="Show open pull requests" Content="Open" />
                                    <app:AppSegmentedItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsStateClosed" AutomationProperties.AutomationId="RepoPullRequestsState_Closed" AutomationProperties.Name="Show closed pull requests" Content="Closed" />
                                    <app:AppSegmentedItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsStateAll" AutomationProperties.AutomationId="RepoPullRequestsState_All" AutomationProperties.Name="Show all pull requests" Content="All" />
                                </app:AppSegmented>
                                <Button
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsNewButton" Grid.Column="2"
                                    MinHeight="{ThemeResource AppDimension34}"
                                    MinWidth="{ThemeResource AppDimension58}"
                                    Padding="{ThemeResource AppPadding12_6}"
                                    AutomationProperties.AutomationId="RepoPullRequestsNewButton"
                                    AutomationProperties.Name="Create pull request"
                                    Click="NewPullRequestButton_Click"
                                    Content="New"
                                    Style="{StaticResource AppAccentButtonStyle}" />
                            </Grid>

                            <Grid Grid.Row="1" ColumnSpacing="{ThemeResource AppGap8}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <AutoSuggestBox
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsSearchBox" x:Name="PullRequestSearchBox"
                                    AutomationProperties.AutomationId="RepoPullRequestsSearchBox"
                                    AutomationProperties.Name="Filter pull requests"
                                    PlaceholderText="Filter pull requests..."
                                    QueryIcon="Find"
                                    Style="{StaticResource AppAutoSuggestBoxStyle}"
                                    TextChanged="PullRequestSearchBox_TextChanged" />
                                <Button
                                    Grid.Column="1"
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsFilterButton"
                                    Width="{ThemeResource AppDimension36}"
                                    Height="{ThemeResource AppDimension36}"
                                    Padding="{ThemeResource AppPadding0}"
                                    AutomationProperties.AutomationId="RepoPullRequestsFilterButton"
                                    AutomationProperties.Name="Pull request filters"
                                    Style="{StaticResource AppToolbarButtonStyle}"
                                    ToolTipService.ToolTip="Filters">
                                    <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize14}" Glyph="&#xE71C;" />
                                    <Button.Flyout>
                                        <Flyout Opened="PullRequestFiltersFlyout_Opened" Placement="BottomEdgeAlignedRight">
                                            <StackPanel Width="{ThemeResource AppDimension260}" Spacing="{ThemeResource AppGap10}">
                                                <TextBlock
                                                    x:Uid="PagesRepoPullRequestPageTextBlockPullRequestFilters"
                                                    Style="{StaticResource ListItemTitleTextBlockStyle}"
                                                    Text="Pull request filters" />
                                                <StackPanel Spacing="{ThemeResource AppGap4}">
                                                    <TextBlock x:Uid="PagesRepoPullRequestPageTextBlockPullRequestSort" Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Sort by" />
                                                    <ComboBox
                                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsSortComboBox" x:Name="PullRequestSortComboBox"
                                                        MinWidth="{ThemeResource AppDimension0}"
                                                        HorizontalAlignment="Stretch"
                                                        AutomationProperties.AutomationId="RepoPullRequestsSortComboBox"
                                                        AutomationProperties.Name="Sort pull requests"
                                                        SelectedIndex="0"
                                                        SelectionChanged="PullRequestFilterComboBox_SelectionChanged"
                                                        Style="{StaticResource AppCompactComboBoxStyle}">
                                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSortUpdated" AutomationProperties.AutomationId="RepoPullRequestsSort_Updated" AutomationProperties.Name="Updated" Content="Updated" />
                                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSortCreated" AutomationProperties.AutomationId="RepoPullRequestsSort_Created" AutomationProperties.Name="Created" Content="Created" />
                                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSortPopularity" AutomationProperties.AutomationId="RepoPullRequestsSort_Popularity" AutomationProperties.Name="Popularity" Content="Popularity" />
                                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSortLongRunning" AutomationProperties.AutomationId="RepoPullRequestsSort_LongRunning" AutomationProperties.Name="Long-running" Content="Long-running" />
                                                    </ComboBox>
                                                </StackPanel>
                                                <StackPanel Spacing="{ThemeResource AppGap4}">
                                                    <TextBlock x:Uid="PagesRepoPullRequestPageTextBlockPullRequestOrder" Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="Order" />
                                                    <ComboBox
                                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsDirectionComboBo" x:Name="PullRequestDirectionComboBox"
                                                        MinWidth="{ThemeResource AppDimension0}"
                                                        HorizontalAlignment="Stretch"
                                                        AutomationProperties.AutomationId="RepoPullRequestsDirectionComboBox"
                                                        AutomationProperties.Name="Pull request sort direction"
                                                        SelectedIndex="0"
                                                        SelectionChanged="PullRequestFilterComboBox_SelectionChanged"
                                                        Style="{StaticResource AppCompactComboBoxStyle}">
                                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsDirectionNewest" AutomationProperties.AutomationId="RepoPullRequestsDirection_Newest" AutomationProperties.Name="Newest first" Content="Newest first" />
                                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsDirectionOldest" AutomationProperties.AutomationId="RepoPullRequestsDirection_Oldest" AutomationProperties.Name="Oldest first" Content="Oldest first" />
                                                    </ComboBox>
                                                </StackPanel>
                                            </StackPanel>
                                        </Flyout>
                                    </Button.Flyout>
                                </Button>
                                <ProgressRing
                                    Grid.Column="2"
                                    Width="{ThemeResource AppDimension24}"
                                    Height="{ThemeResource AppDimension24}"
                                    VerticalAlignment="Center"
                                    IsActive="{Binding IsPullRequestListLoading}"
                                    Visibility="{Binding IsPullRequestListLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </Grid>

                        </Grid>

                        <TextBlock
                            x:Uid="PagesRepoPullRequestPageRepoPullRequestsListScopeNotice" Grid.Row="2"
                            AutomationProperties.AutomationId="RepoPullRequestsListScopeNotice"
                            AutomationProperties.Name="Pull request list scope"
                            Foreground="{ThemeResource AppInkMutedBrush}"
                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                            Text="{Binding PullRequestListScopeNotice}"
                            TextWrapping="WrapWholeWords"
                            Visibility="{Binding HasPullRequestListScopeNotice, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <Grid Grid.Row="1">
                            <StackPanel
                                Style="{StaticResource AppEmptyStatePanelStyle}"
                                Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock x:Uid="PagesRepoPullRequestPageTextBlockNoPullRequestsMatchThis" Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="No pull requests match this view." />
                            </StackPanel>
                            <ListView
                                x:Uid="PagesRepoPullRequestPageRepoPullRequestsList" x:Name="PullRequestsList"
                                AutomationProperties.AutomationId="RepoPullRequestsList"
                                AutomationProperties.Name="Pull requests"
                                Background="{ThemeResource AppTransparentBrush}"
                            BorderThickness="{ThemeResource AppZeroThickness}"
                            ContainerContentChanging="PullRequestsList_ContainerContentChanging"
                            IsItemClickEnabled="True"
                                ItemClick="PullRequestsList_ItemClick"
                            SelectionChanged="PullRequestsList_SelectionChanged"
                            ItemContainerStyle="{StaticResource AppWorkspaceCardListRowStyle}"
                                ItemTemplate="{StaticResource RepoPullRequestListItemTemplate}"
                                ItemsSource="{Binding PullRequests}"
                                SelectedItem="{Binding SelectedPullRequest, Mode=OneWay}"
                                SelectionMode="Single" />
                        </Grid>
                    </Grid>
                </Border>
            </app:AdaptiveWorkspace.LeadingPane>

            <app:AdaptiveWorkspace.PrimaryPane>
                <Border
                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsDetailHost" x:Name="PullRequestDetailHost"
                    AutomationProperties.AccessibilityView="Control"
                    AutomationProperties.AutomationId="RepoPullRequestsDetailHost"
                    AutomationProperties.Name="Pull request detail pane"
                    Style="{StaticResource RepoPullRequestsPanelStyle}">
                    <Grid>
                        <TextBlock
                            x:Uid="PagesRepoPullRequestPageTextBlockSelectAPullRequestToSee" HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                            Text="Select a pull request to see details."
                            Visibility="{Binding IsDetailPlaceholderVisible, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <Grid Visibility="{Binding HasSelectedPullRequest, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid x:Name="PullRequestDetailLayout">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Grid x:Name="PullRequestExpandedHeaderSurface">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <Border
                                        Grid.RowSpan="2"
                                        labs:TransitionHelper.Id="PullRequestHeaderChrome"
                                        Background="{ThemeResource AppSurfaceSubtleBrush}"
                                        BorderBrush="{ThemeResource AppHairlineBrush}"
                                        BorderThickness="{ThemeResource AppBottomHairlineBorderThickness}"
                                        IsHitTestVisible="False" />

                                    <Grid
                                        x:Name="PullRequestDetailHeader"
                                        Padding="{ThemeResource AppPadding10_10_16_12}"
                                        AutomationProperties.AutomationId="RepoPullRequestsDetailHeader"
                                        ColumnSpacing="{ThemeResource AppGap12}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsOpenListPaneButt" x:Name="RepoPullRequestsOpenListPaneButton"
                                    Width="{ThemeResource AppDimension36}"
                                    Height="{ThemeResource AppDimension36}"
                                    Padding="{ThemeResource AppPadding0}"
                                    VerticalAlignment="Top"
                                    labs:TransitionHelper.Id="PullRequestListButton"
                                    AutomationProperties.AutomationId="RepoPullRequestsOpenListPaneButton"
                                    AutomationProperties.Name="Open pull request list"
                                    Click="OpenListPaneButton_Click"
                                    Style="{StaticResource AppToolbarButtonStyle}"
                                    ToolTipService.ToolTip="Pull requests"
                                    Visibility="Collapsed">
                                    <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize14}" Glyph="&#xE8FD;" />
                                </Button>
                                <StackPanel
                                    x:Name="PullRequestDetailTitleHost"
                                    Grid.Column="1"
                                    Spacing="{ThemeResource AppGap7}">
                                    <TextBlock
                                        x:Name="PullRequestDetailTitle"
                                        labs:TransitionHelper.Id="PullRequestTitle"
                                        AutomationProperties.AutomationId="RepoPullRequestsDetailTitle"
                                        FontFamily="{ThemeResource AppDisplayFontFamily}"
                                        FontSize="{ThemeResource AppFontSize25}"
                                        Foreground="{ThemeResource AppInkBrush}"
                                        HorizontalAlignment="Stretch"
                                        MaxLines="2"
                                        Text="{Binding SelectedPullRequestTitle}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="WrapWholeWords"
                                        ToolTipService.ToolTip="{Binding SelectedPullRequestTitle}" />
                                    <Grid x:Name="PullRequestDetailMetadata" ColumnSpacing="{ThemeResource AppGap8}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            FontFamily="{ThemeResource AppMonoFontFamily}"
                                            FontSize="{ThemeResource AppFontSize12}"
                                            Foreground="{ThemeResource AppAccentBrush}"
                                            Text="{Binding SelectedPullRequestNumberText}" />
                                        <TextBlock
                                            Grid.Column="1"
                                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                            Text="{Binding SelectedPullRequestStateText}" />
                                        <TextBlock
                                            Grid.Column="2"
                                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                            Text="{Binding SelectedPullRequestMetadataText}"
                                            TextTrimming="CharacterEllipsis" />
                                    </Grid>
                                </StackPanel>
                                <StackPanel
                                    x:Name="RepoPullRequestsHeaderActions"
                                    Grid.Column="2"
                                    VerticalAlignment="Top"
                                    labs:TransitionHelper.Id="PullRequestActions"
                                    Orientation="Horizontal"
                                    Spacing="{ThemeResource AppGap8}">
                                    <StackPanel
                                        x:Name="RepoPullRequestsInlineActions"
                                        Orientation="Horizontal"
                                        Spacing="{ThemeResource AppGap8}">
                                        <Button
                                            MinHeight="{ThemeResource AppDimension34}"
                                            Padding="{ThemeResource AppPadding12_6}"
                                            AutomationProperties.AutomationId="RepoPullRequestsToggleStateButton"
                                            AutomationProperties.Name="{Binding TogglePullRequestStateButtonText}"
                                            Click="TogglePullRequestStateButton_Click"
                                            IsEnabled="{Binding IsTogglePullRequestStateEnabled}"
                                            Style="{StaticResource AppOutlineButtonStyle}"
                                            ToolTipService.ToolTip="{Binding TogglePullRequestStateButtonText}">
                                            <TextBlock
                                                MaxWidth="{ThemeResource AppDimension128}"
                                                Text="{Binding TogglePullRequestStateButtonText}"
                                                TextTrimming="CharacterEllipsis" />
                                        </Button>
                                        <Button
                                            x:Uid="PagesRepoPullRequestPageRepoPullRequestsSubmitReviewButt" MinHeight="{ThemeResource AppDimension34}"
                                            Padding="{ThemeResource AppPadding12_6}"
                                            AutomationProperties.AutomationId="RepoPullRequestsSubmitReviewButton"
                                            AutomationProperties.Name="Submit pull request review"
                                            Click="SubmitReviewButton_Click"
                                            Content="Review"
                                            IsEnabled="{Binding CanSubmitPullRequestReview}"
                                            Style="{StaticResource AppOutlineButtonStyle}"
                                            ToolTipService.ToolTip="Submit review" />
                                        <DropDownButton
                                            x:Uid="PagesRepoPullRequestPageRepoPullRequestsMergeButton" MinHeight="{ThemeResource AppDimension34}"
                                            Padding="{ThemeResource AppPadding12_6}"
                                            AutomationProperties.AutomationId="RepoPullRequestsMergeButton"
                                            AutomationProperties.Name="Merge pull request options"
                                            Content="Merge"
                                            IsEnabled="{Binding IsMergeEnabled}"
                                            Background="{ThemeResource AppSurfaceBrush}"
                                            BorderBrush="{ThemeResource AppOutlineBrush}"
                                            BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                                            Foreground="{ThemeResource AppInkBrush}">
                                            <DropDownButton.Flyout>
                                                <MenuFlyout>
                                                    <MenuFlyoutItem
                                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsMergeCommitActio" AutomationProperties.AutomationId="RepoPullRequestsMergeCommitAction"
                                                        AutomationProperties.Name="Create merge commit"
                                                        Click="MergeCommitMenuItem_Click"
                                                        IsEnabled="{Binding CanMergeWithMergeCommit}"
                                                        Text="Create merge commit" />
                                                    <MenuFlyoutItem
                                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsSquashMergeActio" AutomationProperties.AutomationId="RepoPullRequestsSquashMergeAction"
                                                        AutomationProperties.Name="Squash and merge"
                                                        Click="SquashMergeMenuItem_Click"
                                                        IsEnabled="{Binding CanMergeWithSquash}"
                                                        Text="Squash and merge" />
                                                    <MenuFlyoutItem
                                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsRebaseMergeActio" AutomationProperties.AutomationId="RepoPullRequestsRebaseMergeAction"
                                                        AutomationProperties.Name="Rebase and merge"
                                                        Click="RebaseMergeMenuItem_Click"
                                                        IsEnabled="{Binding CanMergeWithRebase}"
                                                        Text="Rebase and merge" />
                                                </MenuFlyout>
                                            </DropDownButton.Flyout>
                                        </DropDownButton>
                                    </StackPanel>
                                    <DropDownButton
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactActionsBu" x:Name="RepoPullRequestsCompactActionsButton"
                                        MinHeight="{ThemeResource AppDimension34}"
                                        MinWidth="{ThemeResource AppDimension74}"
                                        Padding="{ThemeResource AppPadding10_6}"
                                        AutomationProperties.AutomationId="RepoPullRequestsCompactActionsButton"
                                        AutomationProperties.Name="More pull request actions"
                                        Content="More"
                                        IsEnabled="{Binding ArePullRequestActionsEnabled}"
                                        Style="{StaticResource AppOutlineButtonStyle}"
                                        ToolTipService.ToolTip="Pull request actions"
                                        Visibility="Collapsed">
                                        <DropDownButton.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem
                                                    AutomationProperties.AutomationId="RepoPullRequestsCompactToggleStateAction"
                                                    AutomationProperties.Name="{Binding TogglePullRequestStateButtonText}"
                                                    Click="TogglePullRequestStateButton_Click"
                                                    IsEnabled="{Binding IsTogglePullRequestStateEnabled}"
                                                    Text="{Binding TogglePullRequestStateButtonText}" />
                                                <MenuFlyoutItem
                                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactSubmitRev" AutomationProperties.AutomationId="RepoPullRequestsCompactSubmitReviewAction"
                                                    AutomationProperties.Name="Submit pull request review"
                                                    Click="SubmitReviewButton_Click"
                                                    IsEnabled="{Binding CanSubmitPullRequestReview}"
                                                    Text="Submit review" />
                                                <MenuFlyoutSeparator />
                                                <MenuFlyoutItem
                                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactMergeComm" AutomationProperties.AutomationId="RepoPullRequestsCompactMergeCommitAction"
                                                    AutomationProperties.Name="Create merge commit"
                                                    Click="MergeCommitMenuItem_Click"
                                                    IsEnabled="{Binding CanMergeWithMergeCommit}"
                                                    Text="Create merge commit" />
                                                <MenuFlyoutItem
                                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactSquashMer" AutomationProperties.AutomationId="RepoPullRequestsCompactSquashMergeAction"
                                                    AutomationProperties.Name="Squash and merge"
                                                    Click="SquashMergeMenuItem_Click"
                                                    IsEnabled="{Binding CanMergeWithSquash}"
                                                    Text="Squash and merge" />
                                                <MenuFlyoutItem
                                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactRebaseMer" AutomationProperties.AutomationId="RepoPullRequestsCompactRebaseMergeAction"
                                                    AutomationProperties.Name="Rebase and merge"
                                                    Click="RebaseMergeMenuItem_Click"
                                                    IsEnabled="{Binding CanMergeWithRebase}"
                                                    Text="Rebase and merge" />
                                            </MenuFlyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                    <Button
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsOpenInspectorPan" x:Name="RepoPullRequestsOpenInspectorPaneButton"
                                        Width="{ThemeResource AppDimension36}"
                                        Height="{ThemeResource AppDimension36}"
                                        Padding="{ThemeResource AppPadding0}"
                                        AutomationProperties.AutomationId="RepoPullRequestsOpenInspectorPaneButton"
                                        AutomationProperties.Name="Open inspector"
                                        Click="OpenInspectorPaneButton_Click"
                                        Style="{StaticResource AppToolbarButtonStyle}"
                                        ToolTipService.ToolTip="Inspector"
                                        Visibility="Collapsed">
                                        <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize14}" Glyph="&#xE9D2;" />
                                    </Button>
                                </StackPanel>
                            </Grid>

                            <app:AppSegmented
                                x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionSegmented" x:Name="PullRequestSectionSegmented"
                                Grid.Row="1"
                                Margin="{ThemeResource AppMargin10_4_10_12}"
                                labs:TransitionHelper.Id="PullRequestSectionSelector"
                                AutomationProperties.AutomationId="RepoPullRequestsSectionSegmented"
                                AutomationProperties.Name="Pull request section"
                                FontSize="{ThemeResource AppFontSize12}"
                                SelectedIndex="0"
                                SelectionChanged="PullRequestSectionSegmented_SelectionChanged"
                                Style="{StaticResource AppContentSizedSegmentedStyle}">
                                <app:AppSegmentedItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionConversat" AutomationProperties.AutomationId="RepoPullRequestsSection_Conversation" AutomationProperties.Name="Show pull request conversation" Content="Conversation" Style="{StaticResource AppContentSizedSegmentedItemStyle}" />
                                <app:AppSegmentedItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionFiles" AutomationProperties.AutomationId="RepoPullRequestsSection_Files" AutomationProperties.Name="Show changed files" Content="Files" Style="{StaticResource AppContentSizedSegmentedItemStyle}" />
                                <app:AppSegmentedItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionCommits" AutomationProperties.AutomationId="RepoPullRequestsSection_Commits" AutomationProperties.Name="Show pull request commits" Content="Commits" Style="{StaticResource AppContentSizedSegmentedItemStyle}" />
                                <app:AppSegmentedItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionReviews" AutomationProperties.AutomationId="RepoPullRequestsSection_Reviews" AutomationProperties.Name="Show pull request reviews" Content="Reviews" Style="{StaticResource AppContentSizedSegmentedItemStyle}" />
                                <app:AppSegmentedItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionTimeline" AutomationProperties.AutomationId="RepoPullRequestsSection_Timeline" AutomationProperties.Name="Show pull request timeline" Content="Timeline" Style="{StaticResource AppContentSizedSegmentedItemStyle}" />
                            </app:AppSegmented>
                                </Grid>

                            <Grid
                                x:Uid="PagesRepoPullRequestPageRepoPullRequestsContentScrollViewer"
                                x:Name="PullRequestContentHost"
                                Grid.Row="1"
                                AutomationProperties.AutomationId="RepoPullRequestsContentScrollViewer"
                                AutomationProperties.Name="Pull request content"
                                Visibility="{Binding IsScrollableContentSectionVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                <ListView
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsCommentsList"
                                    x:Name="PullRequestContentScrollViewer"
                                    Padding="{ThemeResource AppPadding18}"
                                    AutomationProperties.AutomationId="RepoPullRequestsCommentsList"
                                    AutomationProperties.Name="Pull request comments"
                                    ContainerContentChanging="PullRequestDetailList_ContainerContentChanging"
                                    ItemContainerStyle="{StaticResource AppContentListRowStyle}"
                                    ItemTemplate="{StaticResource RepoPullRequestCommentTemplate}"
                                    ItemsPanel="{StaticResource RepoPullRequestVirtualizingItemsPanel}"
                                    ItemsSource="{Binding PullRequestComments}"
                                    SelectionMode="None"
                                    SizeChanged="PullRequestScrollableSection_SizeChanged"
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    ScrollViewer.HorizontalScrollMode="Disabled"
                                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                                    Visibility="{Binding IsConversationSectionVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ListView.Resources>
                                        <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="{ThemeResource AppTransparentColor}" />
                                        <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="{ThemeResource AppTransparentColor}" />
                                        <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="{ThemeResource AppTransparentColor}" />
                                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="{ThemeResource AppTransparentColor}" />
                                    </ListView.Resources>
                                    <ListView.Header>
                                        <StackPanel Margin="{ThemeResource AppMargin0_0_0_16}" Spacing="{ThemeResource AppGap16}">
                                            <Border
                                                Padding="{ThemeResource AppPadding16}"
                                                Background="{ThemeResource AppCanvasInsetBrush}"
                                                BorderBrush="{ThemeResource AppHairlineBrush}"
                                                BorderThickness="{ThemeResource AppHairlineBorderThickness}"
                                                CornerRadius="{ThemeResource AppRadiusSmall}">
                                                <Grid RowSpacing="{ThemeResource AppGap12}">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>
                                                    <Grid ColumnSpacing="{ThemeResource AppGap10}">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="{ThemeResource AppGridLength32}" />
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <common:Avatar
                                                            VerticalAlignment="Center"
                                                            AutomationInstanceId="{Binding SelectedPullRequestAuthorAutomationId}"
                                                            Login="{Binding SelectedPullRequestAuthorLogin}"
                                                            NavigationSource="pull_request_detail_author"
                                                            ShowLogin="False"
                                                            Size="SMALL"
                                                            Url="{Binding SelectedPullRequestAuthorAvatarUrl}" />
                                                        <TextBlock
                                                            Grid.Column="1"
                                                            VerticalAlignment="Center"
                                                            FontFamily="{ThemeResource AppUiFontFamily}"
                                                            FontSize="{ThemeResource AppFontSize13}"
                                                            FontWeight="{ThemeResource AppFontWeightSemibold}"
                                                            Foreground="{ThemeResource AppInkBrush}"
                                                            Text="{Binding SelectedPullRequestAuthorDisplayName}" />
                                                        <TextBlock
                                                            x:Uid="PagesRepoPullRequestPageTextBlockAuthor"
                                                            Grid.Column="2"
                                                            VerticalAlignment="Center"
                                                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                                            Text="Author" />
                                                    </Grid>
                                                    <common:MarkdownViewer
                                                        HostKind="Conversation"
                                                        AutomationInstanceId="RepoPullRequestsBody"
                                                        Grid.Row="1"
                                                        BaseUrl="https://github.com/"
                                                        DocumentSource="{Binding PullRequestBodyMarkdownSource}"
                                                        Text="{Binding PullRequestBodyText}" />
                                                    <common:CommentInteractionBar
                                                        x:Uid="RepoPullRequestsBodyInteractionBar"
                                                        Grid.Row="2"
                                                        AutomationInstanceId="RepoPullRequestsBody"
                                                        AutomationProperties.AutomationId="RepoPullRequestsBodyInteractionBar"
                                                        AutomationProperties.Name="Pull request body actions"
                                                        ActionRequested="CommentInteractionBar_ActionRequested"
                                                        AuthorLogin="{Binding SelectedPullRequestAuthorLogin}"
                                                        Body="{Binding PullRequestBodyText}"
                                                        CanEdit="{Binding CanEditPullRequest}"
                                                        CanReact="{Binding CanReactToPullRequest}"
                                                        CanReply="{Binding IsPullRequestCommentEnabled}"
                                                        HtmlUrl="{Binding SelectedPullRequestHtmlUrl}"
                                                        Reactions="{Binding PullRequestReactions}"
                                                        TargetId="{Binding SelectedPullRequestReactionTargetId}"
                                                        TargetKind="PullRequest"
                                                        ViewerLogin="{Binding AuthenticatedLogin}" />
                                                </Grid>
                                            </Border>
                                            <TextBlock
                                                x:Uid="PagesRepoPullRequestPageTextBlockComments"
                                                FontFamily="{ThemeResource AppDisplayFontFamily}"
                                                FontSize="{ThemeResource AppFontSize20}"
                                                Foreground="{ThemeResource AppInkBrush}"
                                                Text="Comments" />
                                            <TextBlock
                                                Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                                Text="{Binding NoCommentsText}"
                                                Visibility="{Binding IsPullRequestCommentsEmptyVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        </StackPanel>
                                    </ListView.Header>
                                </ListView>

                                <ListView
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsCommitsList"
                                    x:Name="PullRequestCommitsSection"
                                    x:Load="{x:Bind ViewModel.IsCommitsSectionVisible, Mode=OneWay}"
                                    Padding="{ThemeResource AppPadding18}"
                                    AutomationProperties.AutomationId="RepoPullRequestsCommitsList"
                                    AutomationProperties.Name="Pull request commits"
                                    ContainerContentChanging="PullRequestDetailList_ContainerContentChanging"
                                    ItemContainerStyle="{StaticResource AppContentListRowStyle}"
                                    ItemTemplate="{StaticResource RepoPullRequestCommitTemplate}"
                                    ItemsPanel="{StaticResource RepoPullRequestVirtualizingItemsPanel}"
                                    ItemsSource="{Binding PullRequestCommits}"
                                    Loaded="PullRequestScrollableSection_Loaded"
                                    SelectionMode="None"
                                    SizeChanged="PullRequestScrollableSection_SizeChanged"
                                    Unloaded="PullRequestScrollableSection_Unloaded"
                                    Visibility="{Binding IsCommitsSectionVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ListView.Header>
                                        <TextBlock
                                            Margin="{ThemeResource AppMargin0_0_0_12}"
                                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                            Text="{Binding NoCommitsText}"
                                            Visibility="{Binding IsPullRequestCommitsEmptyVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    </ListView.Header>
                                </ListView>

                                <ListView
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsReviewsList"
                                    x:Name="PullRequestReviewsSection"
                                    x:Load="{x:Bind ViewModel.IsReviewsSectionVisible, Mode=OneWay}"
                                    Padding="{ThemeResource AppPadding18}"
                                    AutomationProperties.AutomationId="RepoPullRequestsReviewsList"
                                    AutomationProperties.Name="Pull request reviews"
                                    ContainerContentChanging="PullRequestDetailList_ContainerContentChanging"
                                    ItemContainerStyle="{StaticResource AppContentListRowStyle}"
                                    ItemTemplate="{StaticResource RepoPullRequestReviewTemplate}"
                                    ItemsPanel="{StaticResource RepoPullRequestVirtualizingItemsPanel}"
                                    ItemsSource="{Binding PullRequestReviews}"
                                    Loaded="PullRequestScrollableSection_Loaded"
                                    SelectionMode="None"
                                    SizeChanged="PullRequestScrollableSection_SizeChanged"
                                    Unloaded="PullRequestScrollableSection_Unloaded"
                                    Visibility="{Binding IsReviewsSectionVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ListView.Header>
                                        <TextBlock
                                            Margin="{ThemeResource AppMargin0_0_0_12}"
                                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                            Text="{Binding NoReviewsText}"
                                            Visibility="{Binding IsPullRequestReviewsEmptyVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    </ListView.Header>
                                </ListView>

                                <ListView
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsTimelineList"
                                    x:Name="PullRequestTimelineSection"
                                    x:Load="{x:Bind ViewModel.IsTimelineSectionVisible, Mode=OneWay}"
                                    Padding="{ThemeResource AppPadding18}"
                                    AutomationProperties.AutomationId="RepoPullRequestsTimelineList"
                                    AutomationProperties.Name="Pull request timeline"
                                    ContainerContentChanging="PullRequestDetailList_ContainerContentChanging"
                                    ItemContainerStyle="{StaticResource AppContentListRowStyle}"
                                    ItemTemplate="{StaticResource RepoPullRequestTimelineTemplate}"
                                    ItemsPanel="{StaticResource RepoPullRequestVirtualizingItemsPanel}"
                                    ItemsSource="{Binding PullRequestTimelineEvents}"
                                    Loaded="PullRequestScrollableSection_Loaded"
                                    SelectionMode="None"
                                    SizeChanged="PullRequestScrollableSection_SizeChanged"
                                    Unloaded="PullRequestScrollableSection_Unloaded"
                                    Visibility="{Binding IsTimelineSectionVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ListView.Header>
                                        <TextBlock
                                            Margin="{ThemeResource AppMargin0_0_0_12}"
                                            Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                            Text="{Binding NoTimelineText}"
                                            Visibility="{Binding IsPullRequestTimelineEmptyVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    </ListView.Header>
                                </ListView>
                            </Grid>

                            <Grid
                                x:Name="PullRequestFilesSection"
                                x:Load="{x:Bind ViewModel.IsFilesSectionVisible, Mode=OneWay}"
                                Grid.Row="1"
                                Padding="{ThemeResource AppPadding18_12_18_18}"
                                RowSpacing="{ThemeResource AppGap10}"
                                Loaded="PullRequestScrollableSection_Loaded"
                                SizeChanged="PullRequestScrollableSection_SizeChanged"
                                Unloaded="PullRequestScrollableSection_Unloaded"
                                Visibility="{Binding IsFilesSectionVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid ColumnSpacing="{ThemeResource AppGap8}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" MinWidth="{ThemeResource AppDimension150}" MaxWidth="{ThemeResource AppDimension260}" />
                                        <ColumnDefinition Width="*" MinWidth="{ThemeResource AppDimension150}" MaxWidth="{ThemeResource AppDimension260}" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsFileFilter" AutomationProperties.AutomationId="RepoPullRequestsFileFilter"
                                        AutomationProperties.Name="Filter changed files"
                                        PlaceholderText="Filter files"
                                        Style="{StaticResource AppCommandSearchTextBoxStyle}"
                                        Text="{Binding PullRequestFileFilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBox
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsDiffSearch" Grid.Column="1"
                                        AutomationProperties.AutomationId="RepoPullRequestsDiffSearch"
                                        AutomationProperties.Name="Search pull request diff"
                                        PlaceholderText="Search diff"
                                        Style="{StaticResource AppCommandSearchTextBoxStyle}"
                                        Text="{Binding PullRequestDiffSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock
                                        Grid.Column="2"
                                        VerticalAlignment="Center"
                                        Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                        Text="{Binding PullRequestDiffSearchMatchCountText}" />
                                    <Button
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsPreviousDiffMatc" Grid.Column="3"
                                        Width="{ThemeResource AppDimension36}"
                                        Height="{ThemeResource AppDimension36}"
                                        Padding="{ThemeResource AppPadding0}"
                                        AutomationProperties.AutomationId="RepoPullRequestsPreviousDiffMatch"
                                        AutomationProperties.Name="Previous diff match"
                                        Click="PreviousDiffMatchButton_Click"
                                        IsEnabled="{Binding HasPullRequestDiffSearchMatches}"
                                        Style="{StaticResource AppToolbarButtonStyle}"
                                        ToolTipService.ToolTip="Previous match">
                                        <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize13}" Glyph="&#xE70E;" />
                                    </Button>
                                    <Button
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsNextDiffMatch" Grid.Column="4"
                                        Width="{ThemeResource AppDimension36}"
                                        Height="{ThemeResource AppDimension36}"
                                        Padding="{ThemeResource AppPadding0}"
                                        AutomationProperties.AutomationId="RepoPullRequestsNextDiffMatch"
                                        AutomationProperties.Name="Next diff match"
                                        Click="NextDiffMatchButton_Click"
                                        IsEnabled="{Binding HasPullRequestDiffSearchMatches}"
                                        Style="{StaticResource AppToolbarButtonStyle}"
                                        ToolTipService.ToolTip="Next match">
                                        <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize13}" Glyph="&#xE70D;" />
                                    </Button>
                                </Grid>
                                <commit:CommitDiffViewer
                                    Grid.Row="1"
                                    ActionCompleted="CommitDiffViewer_ActionCompleted"
                                    AutomationProperties.AutomationId="RepoPullRequestsDiffViewer"
                                    RowProjection="{Binding PullRequestDiffRowProjection}"
                                    SelectedSearchMatchIndex="{Binding SelectedPullRequestDiffSearchMatchIndex}" />
                            </Grid>

                            <Border
                                x:Name="PullRequestCommentFormHost"
                                Grid.Row="2"
                                Padding="{ThemeResource AppPadding12_8}"
                                BorderBrush="{ThemeResource AppHairlineBrush}"
                                BorderThickness="{ThemeResource AppTopHairlineBorderThickness}"
                                Visibility="{Binding IsConversationSectionVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Button
                                    x:Uid="RepoPullRequestsOpenCommentButton"
                                    x:Name="RepoPullRequestsOpenCompactCommentButton"
                                    MinHeight="{ThemeResource AppDimension36}"
                                    MinWidth="{ThemeResource AppDimension112}"
                                    HorizontalAlignment="Right"
                                    AutomationProperties.AutomationId="RepoPullRequestsOpenCompactCommentButton"
                                    AutomationProperties.Name="Open pull request comment editor"
                                    IsEnabled="{Binding IsPullRequestCommentEnabled}"
                                    Style="{StaticResource AppAccentButtonStyle}">
                                    <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                                        <SymbolIcon VerticalAlignment="Center" Foreground="{ThemeResource AppAccentForegroundBrush}" Symbol="Message" />
                                        <TextBlock
                                            x:Uid="RepoPullRequestsOpenCommentLabel"
                                            VerticalAlignment="Center"
                                            Foreground="{ThemeResource AppAccentForegroundBrush}"
                                            Text="Comment" />
                                    </StackPanel>
                                    <Button.Flyout>
                                        <Flyout
                                            x:Name="PullRequestCommentFlyout"
                                            Closed="CompactCommentFlyout_Closed"
                                            Opened="CompactCommentFlyout_Opened"
                                            Placement="TopEdgeAlignedRight">
                                            <Flyout.FlyoutPresenterStyle>
                                                <Style TargetType="FlyoutPresenter">
                                                    <Setter Property="Padding" Value="{ThemeResource AppPadding0}" />
                                                    <Setter Property="MinWidth" Value="{ThemeResource AppDimension0}" />
                                                    <Setter Property="MaxWidth" Value="{ThemeResource AppDimension460}" />
                                                </Style>
                                            </Flyout.FlyoutPresenterStyle>
                                            <Border Width="{ThemeResource AppDimension440}" Padding="{ThemeResource AppPadding16}">
                                                <common:MarkdownForm
                                                    x:Name="PullRequestCompactCommentForm"
                                                    AutomationProperties.AutomationId="RepoPullRequestsCompactCommentBox"
                                                    AutomationInstanceId="RepoPullRequestsCompactCommentBox"
                                                    DocumentSource="{Binding PullRequestCommentMarkdownSource}"
                                                    EditorHeight="140"
                                                    Text="{Binding PullRequestCommentDraft, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                    <common:MarkdownForm.ActionContent>
                                                        <Button
                                                            x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactCommentBu" MinHeight="{ThemeResource AppDimension34}"
                                                            MinWidth="{ThemeResource AppDimension96}"
                                                            HorizontalAlignment="Right"
                                                            AutomationProperties.AutomationId="RepoPullRequestsCompactCommentButton"
                                                            AutomationProperties.Name="Post pull request comment"
                                                            Click="CommentButton_Click"
                                                            Content="Comment"
                                                            IsEnabled="{Binding IsPullRequestCommentEnabled}"
                                                            Style="{StaticResource AppAccentButtonStyle}" />
                                                    </common:MarkdownForm.ActionContent>
                                                </common:MarkdownForm>
                                            </Border>
                                        </Flyout>
                                    </Button.Flyout>
                                </Button>
                            </Border>
                            </Grid>

                            <Grid
                                x:Name="PullRequestShyHeaderSurface"
                                VerticalAlignment="Top"
                                AutomationProperties.AutomationId="RepoPullRequestsShyHeader"
                                Canvas.ZIndex="10"
                                Visibility="Collapsed">
                                <Border
                                    labs:TransitionHelper.Id="PullRequestHeaderChrome"
                                    Background="{ThemeResource AppTransientOverlayBrush}"
                                    BorderBrush="{ThemeResource AppHairlineBrush}"
                                    BorderThickness="{ThemeResource AppBottomHairlineBorderThickness}"
                                    IsHitTestVisible="False" />

                                <Grid
                                    Padding="{ThemeResource AppPadding8_6_10_6}"
                                    ColumnSpacing="{ThemeResource AppGap10}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                <Button
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsOpenListPaneButt"
                                    x:Name="RepoPullRequestsShyOpenListPaneButton"
                                    Width="{ThemeResource AppDimension36}"
                                    Height="{ThemeResource AppDimension36}"
                                    Padding="{ThemeResource AppPadding0}"
                                    VerticalAlignment="Center"
                                    labs:TransitionHelper.Id="PullRequestListButton"
                                    AutomationProperties.AutomationId="RepoPullRequestsOpenListPaneButton"
                                    AutomationProperties.Name="Open pull request list"
                                    Click="OpenListPaneButton_Click"
                                    Style="{StaticResource AppToolbarButtonStyle}"
                                    ToolTipService.ToolTip="Pull requests"
                                    Visibility="Collapsed">
                                    <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize14}" Glyph="&#xE8FD;" />
                                </Button>

                                <TextBlock
                                    x:Name="PullRequestShyDetailTitle"
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    labs:TransitionHelper.Id="PullRequestTitle"
                                    AutomationProperties.AutomationId="RepoPullRequestsDetailTitle"
                                    FontFamily="{ThemeResource AppDisplayFontFamily}"
                                    FontSize="{ThemeResource AppFontSize18}"
                                    Foreground="{ThemeResource AppInkBrush}"
                                    MaxLines="1"
                                    Text="{Binding SelectedPullRequestTitle}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap"
                                    ToolTipService.ToolTip="{Binding SelectedPullRequestTitle}" />

                                <StackPanel
                                    x:Name="RepoPullRequestsShyHeaderActions"
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    labs:TransitionHelper.Id="PullRequestActions"
                                    Orientation="Horizontal"
                                    Spacing="{ThemeResource AppGap8}">
                                    <ComboBox
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionSegmented"
                                        x:Name="PullRequestShySectionComboBox"
                                        Width="{ThemeResource AppDimension164}"
                                        labs:TransitionHelper.Id="PullRequestSectionSelector"
                                        AutomationProperties.AutomationId="RepoPullRequestsShySectionComboBox"
                                        AutomationProperties.Name="Pull request section"
                                        SelectedIndex="0"
                                        SelectionChanged="PullRequestShySectionComboBox_SelectionChanged"
                                        Style="{StaticResource AppCompactComboBoxStyle}">
                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionConversat" AutomationProperties.AutomationId="RepoPullRequestsShySection_Conversation" AutomationProperties.Name="Show pull request conversation" Content="Conversation" />
                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionFiles" AutomationProperties.AutomationId="RepoPullRequestsShySection_Files" AutomationProperties.Name="Show changed files" Content="Files" />
                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionCommits" AutomationProperties.AutomationId="RepoPullRequestsShySection_Commits" AutomationProperties.Name="Show pull request commits" Content="Commits" />
                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionReviews" AutomationProperties.AutomationId="RepoPullRequestsShySection_Reviews" AutomationProperties.Name="Show pull request reviews" Content="Reviews" />
                                        <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionTimeline" AutomationProperties.AutomationId="RepoPullRequestsShySection_Timeline" AutomationProperties.Name="Show pull request timeline" Content="Timeline" />
                                    </ComboBox>
                                    <DropDownButton
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactActionsBu"
                                        x:Name="RepoPullRequestsShyActionsButton"
                                        MinHeight="{ThemeResource AppDimension34}"
                                        MinWidth="{ThemeResource AppDimension74}"
                                        Padding="{ThemeResource AppPadding10_6}"
                                        AutomationProperties.AutomationId="RepoPullRequestsCompactActionsButton"
                                        AutomationProperties.Name="More pull request actions"
                                        Content="More"
                                        IsEnabled="{Binding ArePullRequestActionsEnabled}"
                                        Style="{StaticResource AppOutlineButtonStyle}"
                                        ToolTipService.ToolTip="Pull request actions">
                                        <DropDownButton.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem AutomationProperties.AutomationId="RepoPullRequestsCompactToggleStateAction" AutomationProperties.Name="{Binding TogglePullRequestStateButtonText}" Click="TogglePullRequestStateButton_Click" IsEnabled="{Binding IsTogglePullRequestStateEnabled}" Text="{Binding TogglePullRequestStateButtonText}" />
                                                <MenuFlyoutItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactSubmitRev" AutomationProperties.AutomationId="RepoPullRequestsCompactSubmitReviewAction" AutomationProperties.Name="Submit pull request review" Click="SubmitReviewButton_Click" IsEnabled="{Binding CanSubmitPullRequestReview}" Text="Submit review" />
                                                <MenuFlyoutSeparator />
                                                <MenuFlyoutItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactMergeComm" AutomationProperties.AutomationId="RepoPullRequestsCompactMergeCommitAction" AutomationProperties.Name="Create merge commit" Click="MergeCommitMenuItem_Click" IsEnabled="{Binding CanMergeWithMergeCommit}" Text="Create merge commit" />
                                                <MenuFlyoutItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactSquashMer" AutomationProperties.AutomationId="RepoPullRequestsCompactSquashMergeAction" AutomationProperties.Name="Squash and merge" Click="SquashMergeMenuItem_Click" IsEnabled="{Binding CanMergeWithSquash}" Text="Squash and merge" />
                                                <MenuFlyoutItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsCompactRebaseMer" AutomationProperties.AutomationId="RepoPullRequestsCompactRebaseMergeAction" AutomationProperties.Name="Rebase and merge" Click="RebaseMergeMenuItem_Click" IsEnabled="{Binding CanMergeWithRebase}" Text="Rebase and merge" />
                                            </MenuFlyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                    <Button
                                        x:Uid="PagesRepoPullRequestPageRepoPullRequestsOpenInspectorPan"
                                        x:Name="RepoPullRequestsShyOpenInspectorPaneButton"
                                        Width="{ThemeResource AppDimension36}"
                                        Height="{ThemeResource AppDimension36}"
                                        Padding="{ThemeResource AppPadding0}"
                                        AutomationProperties.AutomationId="RepoPullRequestsOpenInspectorPaneButton"
                                        AutomationProperties.Name="Open inspector"
                                        Click="OpenInspectorPaneButton_Click"
                                        Style="{StaticResource AppToolbarButtonStyle}"
                                        ToolTipService.ToolTip="Inspector"
                                        Visibility="Collapsed">
                                        <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize14}" Glyph="&#xE9D2;" />
                                    </Button>
                                </StackPanel>

                                <ComboBox
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionSegmented"
                                    x:Name="PullRequestSectionComboBox"
                                    Grid.Row="1"
                                    Grid.ColumnSpan="3"
                                    Margin="{ThemeResource AppMargin0_6_0_0}"
                                    HorizontalAlignment="Stretch"
                                    AutomationProperties.AutomationId="RepoPullRequestsSectionComboBox"
                                    AutomationProperties.Name="Pull request section"
                                    SelectedIndex="0"
                                    SelectionChanged="PullRequestSectionComboBox_SelectionChanged"
                                    Style="{StaticResource AppCompactComboBoxStyle}"
                                    Visibility="Collapsed">
                                    <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionConversat" AutomationProperties.AutomationId="RepoPullRequestsCompactSection_Conversation" AutomationProperties.Name="Show pull request conversation" Content="Conversation" />
                                    <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionFiles" AutomationProperties.AutomationId="RepoPullRequestsCompactSection_Files" AutomationProperties.Name="Show changed files" Content="Files" />
                                    <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionCommits" AutomationProperties.AutomationId="RepoPullRequestsCompactSection_Commits" AutomationProperties.Name="Show pull request commits" Content="Commits" />
                                    <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionReviews" AutomationProperties.AutomationId="RepoPullRequestsCompactSection_Reviews" AutomationProperties.Name="Show pull request reviews" Content="Reviews" />
                                    <ComboBoxItem x:Uid="PagesRepoPullRequestPageRepoPullRequestsSectionTimeline" AutomationProperties.AutomationId="RepoPullRequestsCompactSection_Timeline" AutomationProperties.Name="Show pull request timeline" Content="Timeline" />
                                </ComboBox>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </Border>
            </app:AdaptiveWorkspace.PrimaryPane>

            <app:AdaptiveWorkspace.TrailingPane>
                <Border
                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsInspector" x:Name="PullRequestInspectorHost"
                    AutomationProperties.AccessibilityView="Control"
                    AutomationProperties.AutomationId="RepoPullRequestsInspector"
                    AutomationProperties.Name="Pull request inspector"
                    Padding="{ThemeResource AppPadding16_6_10_16}"
                    Style="{StaticResource RepoPullRequestsPanelStyle}">
                    <ScrollViewer
                        HorizontalScrollBarVisibility="Disabled"
                        VerticalScrollBarVisibility="Auto">
                        <StackPanel Spacing="{ThemeResource AppGap18}">
                            <Grid ColumnSpacing="{ThemeResource AppGap8}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockInspector" VerticalAlignment="Center"
                                    FontFamily="{ThemeResource AppDisplayFontFamily}"
                                    FontSize="{ThemeResource AppFontSize21}"
                                    Foreground="{ThemeResource AppInkBrush}"
                                    Text="Inspector" />
                                <Button
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsCloseInspectorPa" x:Name="RepoPullRequestsCloseInspectorPaneButton"
                                    Grid.Column="1"
                                    Width="{ThemeResource AppDimension36}"
                                    Height="{ThemeResource AppDimension36}"
                                    Padding="{ThemeResource AppPadding0}"
                                    AutomationProperties.AutomationId="RepoPullRequestsCloseInspectorPaneButton"
                                    AutomationProperties.Name="Close inspector"
                                    Click="CloseWorkspaceDrawerButton_Click"
                                    Style="{StaticResource AppToolbarButtonStyle}"
                                    ToolTipService.ToolTip="Close inspector"
                                    Visibility="Collapsed">
                                    <FontIcon FontFamily="{StaticResource SegoeFluentIcons}" FontSize="{ThemeResource AppFontSize14}" Glyph="&#xE9D2;" />
                                </Button>
                            </Grid>

                            <StackPanel Spacing="{ThemeResource AppGap8}">
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockStatus" FontFamily="{ThemeResource AppUiFontFamily}"
                                    FontSize="{ThemeResource AppFontSize13}"
                                    FontWeight="{ThemeResource AppFontWeightSemibold}"
                                    Foreground="{ThemeResource AppInkBrush}"
                                    Text="Status" />
                                <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                                    <Ellipse
                                        Width="{ThemeResource AppDimension8}"
                                        Height="{ThemeResource AppDimension8}"
                                        VerticalAlignment="Center"
                                        Fill="{ThemeResource AppAccentBrush}" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                        Text="{Binding SelectedPullRequestStateText}" />
                                </StackPanel>
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="{Binding MergeStatusText}" TextWrapping="WrapWholeWords" />
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="{Binding BranchSummaryText}" TextWrapping="WrapWholeWords" />
                            </StackPanel>

                            <StackPanel Spacing="{ThemeResource AppGap8}">
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockReviewers" FontFamily="{ThemeResource AppUiFontFamily}"
                                    FontSize="{ThemeResource AppFontSize13}"
                                    FontWeight="{ThemeResource AppFontWeightSemibold}"
                                    Foreground="{ThemeResource AppInkBrush}"
                                    Text="Reviewers" />
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockNoReviewersRequested" Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                    Text="No reviewers requested"
                                    Visibility="{Binding HasNoRequestedReviewers, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <ItemsControl
                                    ItemTemplate="{StaticResource RepoPullRequestReviewerActorTemplate}"
                                    ItemsSource="{Binding RequestedReviewers}"
                                    Visibility="{Binding HasRequestedReviewers, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </StackPanel>

                            <StackPanel Spacing="{ThemeResource AppGap8}">
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockAssignees" FontFamily="{ThemeResource AppUiFontFamily}"
                                    FontSize="{ThemeResource AppFontSize13}"
                                    FontWeight="{ThemeResource AppFontWeightSemibold}"
                                    Foreground="{ThemeResource AppInkBrush}"
                                    Text="Assignees" />
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockNoOneAssigned" Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                    Text="No one assigned"
                                    Visibility="{Binding HasNoAssignees, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <ItemsControl
                                    ItemTemplate="{StaticResource RepoPullRequestAssigneeActorTemplate}"
                                    ItemsSource="{Binding SelectedAssignees}"
                                    Visibility="{Binding HasAssignees, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </StackPanel>

                            <StackPanel Spacing="{ThemeResource AppGap8}">
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockLabels" FontFamily="{ThemeResource AppUiFontFamily}"
                                    FontSize="{ThemeResource AppFontSize13}"
                                    FontWeight="{ThemeResource AppFontWeightSemibold}"
                                    Foreground="{ThemeResource AppInkBrush}"
                                    Text="Labels" />
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockNoLabels" Style="{StaticResource SectionSecondaryTextBlockStyle}"
                                    Text="No labels"
                                    Visibility="{Binding HasNoLabels, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <ItemsControl
                                    ItemTemplate="{StaticResource RepoPullRequestLabelTemplate}"
                                    ItemsSource="{Binding SelectedLabels}"
                                    Visibility="{Binding HasLabels, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </StackPanel>

                            <StackPanel Spacing="{ThemeResource AppGap8}">
                                <TextBlock
                                    x:Uid="PagesRepoPullRequestPageTextBlockMilestone" FontFamily="{ThemeResource AppUiFontFamily}"
                                    FontSize="{ThemeResource AppFontSize13}"
                                    FontWeight="{ThemeResource AppFontWeightSemibold}"
                                    Foreground="{ThemeResource AppInkBrush}"
                                    Text="Milestone" />
                                <TextBlock Style="{StaticResource SectionSecondaryTextBlockStyle}" Text="{Binding MilestoneTitle}" TextWrapping="WrapWholeWords" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Spacing="{ThemeResource AppGap8}">
                                <Button
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsEditButton" MinHeight="{ThemeResource AppDimension34}"
                                    Padding="{ThemeResource AppPadding12_6}"
                                    AutomationProperties.AutomationId="RepoPullRequestsEditButton"
                                    AutomationProperties.Name="Edit pull request"
                                    Click="EditPullRequestButton_Click"
                                    Content="Edit"
                                    IsEnabled="{Binding CanEditPullRequest}"
                                    Style="{StaticResource AppOutlineButtonStyle}" />
                                <Button
                                    x:Uid="PagesRepoPullRequestPageRepoPullRequestsMetadataButton" MinHeight="{ThemeResource AppDimension34}"
                                    Padding="{ThemeResource AppPadding12_6}"
                                    AutomationProperties.AutomationId="RepoPullRequestsMetadataButton"
                                    AutomationProperties.Name="Edit pull request metadata"
                                    Click="MetadataButton_Click"
                                    Content="Metadata"
                                    IsEnabled="{Binding CanManagePullRequestMetadata}"
                                    Style="{StaticResource AppOutlineButtonStyle}" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </app:AdaptiveWorkspace.TrailingPane>
        </app:AdaptiveWorkspace>
    </Grid>
</Page>

