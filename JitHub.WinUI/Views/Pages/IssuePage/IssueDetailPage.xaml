<Page
    x:Class="JitHub.WinUI.Views.Pages.IssuePage.IssueDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JitHub.WinUI.Views.Pages.IssuePage"
    xmlns:common="using:JitHub.WinUI.Views.Controls.Common"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:JitHub.WinUI.Views.Controls"
    xmlns:appcontrols="using:JitHub.WinUI.Views.Controls.App"
    mc:Ignorable="d"
    SizeChanged="Page_SizeChanged"
    Background="Transparent">
    
    <Grid
        Padding="8">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideLayout">
                    <VisualState.Setters>
                        <Setter Target="SidePane.(Grid.Column)" Value="1"/>
                        <Setter Target="SidePane.(Grid.RowSpan)" Value="2"/>
                        <Setter Target="CommentsPane.(Grid.Column)" Value="0"/>
                        <Setter Target="CommentsPane.(Grid.RowSpan)" Value="2"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowLayout">
                    <VisualState.Setters>
                        <Setter Target="SidePane.(Grid.Row)" Value="0"/>
                        <Setter Target="SidePane.(Grid.ColumnSpan)" Value="2"/>
                        <Setter Target="CommentsPane.(Grid.Row)" Value="1"/>
                        <Setter Target="CommentsPane.(Grid.ColumnSpan)" Value="2"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid
            Grid.Row="0"
            Margin="0 0 0 8">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid
                Grid.Row="0"
                HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock
                    VerticalAlignment="Top"
                    Grid.Column="0"
                    Style="{StaticResource SectionTitleTextBlockStyle}"
                    IsTextSelectionEnabled="True"
                    d:Text="New Bug Title"
                    TextWrapping="Wrap">
                    <Run
                        Text="{x:Bind ViewModel.Issue.Title, Mode=OneWay}"/>
                    <Run
                        Foreground="{ThemeResource AppInkMutedBrush}"
                        Text="{x:Bind ViewModel.Issue.Number, Converter={StaticResource NumberToHashTaggedStringConverter}, Mode=OneWay}"/>
                </TextBlock>
                <Button
                    Visibility="{x:Bind ViewModel.IsAuthor, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Right"
                    Grid.Column="1"
                    Style="{StaticResource AppToolbarButtonStyle}"
                    Command="{x:Bind ViewModel.EditCommand, Mode=OneWay}">
                    Edit
                </Button>
            </Grid>
            <Grid
                Padding="0 0 0 4"
                Grid.Row="1"
                BorderBrush="{ThemeResource AppOutlineBrush}"
                BorderThickness="0 0 0 1">
                <StackPanel
                    Orientation="Horizontal"
                    Spacing="4">
                    <Border
                        CornerRadius="{ThemeResource AppRadiusSmall}"
                        Height="24"
                        MinWidth="48"
                        Padding="8 0"
                        VerticalAlignment="Center"
                        Width="auto"
                        d:Background="#4cd134"
                        BorderThickness="0"
                        Background="{x:Bind GetStateBackgroundBrush(ViewModel.Issue.State), Mode=OneWay}">
                        <TextBlock
                            Style="{StaticResource AppStatusPillTextBlockStyle}"
                            d:Text="Open"
                            Text="{x:Bind ViewModel.Issue.State.StringValue, Mode=OneWay}"/>
                    </Border>
                    <TextBlock
                        VerticalAlignment="Center"
                        Style="{StaticResource SectionSecondaryTextBlockStyle}"
                        TextWrapping="Wrap"
                        Foreground="{ThemeResource AppInkMutedBrush}">
                        <Run
                            FontWeight="SemiBold"
                            d:Text="nerocui"
                            Text="{x:Bind ViewModel.Issue.User.Login, Mode=OneWay}"/>
                        <Run
                            d:Text="opened this issue on Mar 11, 2020"
                            Text="{x:Bind ViewModel.Issue.CreatedAt, Converter={StaticResource OpenDateToStringForDetailConverter}, ConverterParameter=issue, Mode=OneWay}"/>
                        <Run
                            d:Text=" · 15 comments"
                            Text="{x:Bind ViewModel.Issue.Comments, Converter={StaticResource NumberOfCommentsToDescriptionDetailConverter}, Mode=OneWay}"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Grid>
        <Grid
            Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ScrollViewer
                x:Name="CommentsPane"
                Padding="0 0 12 0">
                <StackPanel
                    Spacing="8">
                    <controls:UserCommentBlock
                        Visibility="{x:Bind ViewModel.Issue.Body, Converter={StaticResource StringToVisibilityConverter}, Mode=OneWay}"
                        ViewModel="{x:Bind ViewModel.BodyViewModel, Mode=OneWay}"/>
                    <muxc:ProgressBar
                        Height="2"
                        Margin="56,0,0,0"
                        Background="{ThemeResource AppSurfaceSubtleBrush}"
                        Foreground="{ThemeResource AppAccentBrush}"
                        IsIndeterminate="True"
                        Visibility="{x:Bind ViewModel.IsCommentsLoading, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}" />
                    <muxc:ItemsRepeater
                        ItemsSource="{x:Bind ViewModel.Comments, Mode=OneWay}">
                        <muxc:ItemsRepeater.Layout>
                            <muxc:StackLayout Spacing="8"/>
                        </muxc:ItemsRepeater.Layout>
                        <muxc:ItemsRepeater.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <controls:UserCommentBlock
                                        ViewModel="{Binding Mode=OneWay}"/>
                                </Grid>
                            </DataTemplate>
                        </muxc:ItemsRepeater.ItemTemplate>
                    </muxc:ItemsRepeater>
                    <Grid
                        MaxWidth="{Binding ActualWidth, ElementName=CommentsPane}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <common:Avatar
                            Grid.Column="0"
                            VerticalAlignment="Top"
                            Margin="0 0 12 0"
                            Size="BIG"
                            ShowLogin="False"
                            Url="{x:Bind ViewModel.User.AvatarUrl, Mode=OneWay}"/>
                        <common:MarkdownForm
                            Grid.Column="1"
                            HorizontalAlignment="Stretch"
                            Text="{x:Bind ViewModel.Text, Mode=TwoWay}">
                            <common:MarkdownForm.ActionContent>
                                <StackPanel
                                    Style="{StaticResource ActionPanel}">
                                    <Button
                                        Command="{x:Bind ViewModel.CloseCommand, Mode=OneWay}"
                                        Content="{x:Bind ViewModel.CloseButtonText, Mode=OneWay}"
                                        Style="{StaticResource AppDangerOutlineButtonStyle}"
                                        Visibility="{x:Bind ViewModel.IsUserCollaborator, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"/>
                                    <Button
                                        Command="{x:Bind ViewModel.CommentCommand, Mode=OneWay}"
                                        Style="{StaticResource AppAccentButtonStyle}"
                                        Content="Comment"/>
                                </StackPanel>
                            </common:MarkdownForm.ActionContent>
                        </common:MarkdownForm>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
            <common:IssueSidePanel
                x:Name="SidePane"
                ViewModel="{x:Bind ViewModel.SideBarViewModel, Mode=OneWay}"/>
        </Grid>
    </Grid>
</Page>



