<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="using:JitHub.WinUI.Views.Controls.Common">
    <DataTemplate x:Key="UnpinnedEventTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <common:Avatar
                AutomationInstanceId="{Binding ActorAutomationId, Mode=OneWay}"
                NavigationSource="legacy_issue_timeline_actor"
                Grid.Column="0"
                HorizontalAlignment="Center"
                Login="{Binding Actor.Login, Mode=OneWay}"
                Url="{Binding Actor.AvatarUrl, Mode=OneWay}" />
            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="4">
                <TextBlock Text="{Binding Actor.Login, Mode=OneWay}" />
                <TextBlock x:Uid="DataTemplatesIssueEventUnpinnedEventTextBlockUnpinnedThi" Text="unpinned this issue" />
                <TextBlock Text="{Binding CreatedAt, Converter={StaticResource TimeAgoConverter}, ConverterParameter='', Mode=OneWay}" />
            </StackPanel>
        </Grid>
    </DataTemplate>
</ResourceDictionary>



